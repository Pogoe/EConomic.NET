// <auto-generated>
//     Generated by tools/EConomic.SpecConverter (json-context).
//     Regenerate after running NSwag; do not edit by hand.
// </auto-generated>
#nullable enable

using System.Text.Json;
using System.Text.Json.Serialization;

namespace EConomic.Open.Generated.Dimensions;

/// <summary>
/// Source-generated serialization metadata for the generated legacy clients, so the
/// package stays trim- and AOT-compatible despite NSwag emitting reflection-based calls.
/// </summary>
[JsonSourceGenerationOptions(
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
    PropertyNameCaseInsensitive = true)]
[JsonSerializable(typeof(AccountCreatedResult))]
[JsonSerializable(typeof(AccountCreatedResultArray))]
[JsonSerializable(typeof(AccountCursorResults))]
[JsonSerializable(typeof(BookedEntryCreatedResult))]
[JsonSerializable(typeof(BookedEntryCreatedResultArray))]
[JsonSerializable(typeof(BookedEntryCursorResults))]
[JsonSerializable(typeof(BookedInvoiceLineCursorResults))]
[JsonSerializable(typeof(BudgetFigureCreatedResult))]
[JsonSerializable(typeof(BudgetFigureCreatedResultArray))]
[JsonSerializable(typeof(BudgetFigureCursorResults))]
[JsonSerializable(typeof(CreatedResult))]
[JsonSerializable(typeof(Dimension))]
[JsonSerializable(typeof(DimensionCursorResults))]
[JsonSerializable(typeof(DimensionDistribution))]
[JsonSerializable(typeof(DimensionDistributionCursorResults))]
[JsonSerializable(typeof(DimensionValue))]
[JsonSerializable(typeof(DimensionValueCreatedResult))]
[JsonSerializable(typeof(DimensionValueCursorResults))]
[JsonSerializable(typeof(DraftEntryCreatedResult))]
[JsonSerializable(typeof(DraftEntryCreatedResultArray))]
[JsonSerializable(typeof(DraftEntryCursorResults))]
[JsonSerializable(typeof(SalesDocumentLineCreatedResult))]
[JsonSerializable(typeof(SalesDocumentLineCreatedResultArray))]
[JsonSerializable(typeof(SalesDocumentLineCursorResults))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<Account>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<BookedEntry>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<BookedEntryDimensionChangeLog>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<BookedInvoiceLine>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<BudgetFigure>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<Dimension>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<DimensionDistribution>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<DimensionValue>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<DraftEntry>))]
[JsonSerializable(typeof(System.Collections.Generic.ICollection<SalesDocumentLine>))]
[JsonSerializable(typeof(int))]
[JsonSerializable(typeof(object))]
internal sealed partial class EconomicOpenDimensionsJsonContext : JsonSerializerContext;

/// <summary>
/// Points each generated client at the source-generated resolver. NSwag declares
/// <c>UpdateJsonSerializerSettings</c> as a partial hook for exactly this purpose.
/// </summary>
internal partial class AccountClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class BookedEntryClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class BookedInvoiceLineClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class BudgetFigureClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class DimensionDistributionsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class DimensionValuesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class DimensionsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class DraftEntryClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

internal partial class SalesDocumentLineClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicOpenDimensionsJsonContext.Default);
    }
}

