// <auto-generated>
//     Generated by tools/EConomic.SpecConverter (open-facade).
//     Regenerate after a spec refresh; do not edit by hand.
// </auto-generated>
#nullable enable

using EConomic.Querying;
using Raw = EConomic.Open.Generated.Projects;

namespace EConomic.Open;

/// <summary>
/// A resource from <c>/Activities</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectActivity
{
    /// <summary>The <c>number</c> field.</summary>
    public required int Number { get; init; }

    /// <summary>The <c>groupNumber</c> field.</summary>
    public required int GroupNumber { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>isBarred</c> field.</summary>
    public bool? IsBarred { get; init; }

    /// <summary>The <c>inLieuCode</c> field.</summary>
    public int? InLieuCode { get; init; }

    /// <summary>The <c>hideInSearch</c> field.</summary>
    public bool? HideInSearch { get; init; }

    /// <summary>The <c>cutoffDate</c> field.</summary>
    public System.DateTimeOffset? CutoffDate { get; init; }

    /// <summary>The <c>salesPriceBefore</c> field.</summary>
    public double? SalesPriceBefore { get; init; }

    /// <summary>The <c>salesPriceAfter</c> field.</summary>
    public double? SalesPriceAfter { get; init; }

    /// <summary>The <c>costPriceMarkupPercentage</c> field.</summary>
    public double? CostPriceMarkupPercentage { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/Activities</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectActivityFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>groupNumber</c>.</summary>
    [EconomicField("groupNumber")]
    public NumericField<int> GroupNumber { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>isBarred</c>.</summary>
    [EconomicField("isBarred")]
    public EqualityField<bool> IsBarred { get; } = null!;
}

/// <summary>
/// What <c>/Activities</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectActivitySort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/Activities</c> and maps it to <see cref="ProjectActivity"/>.</summary>
internal sealed class ProjectActivitySource(Raw.ActivitiesClient client)
    : IEconomicOpenSource<ProjectActivity>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectActivity>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllActivitiesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /Activities").ConfigureAwait(false);

        var items = new List<ProjectActivity>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectActivity>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectActivity>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfActivitiesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /Activities/paged").ConfigureAwait(false);

        var items = new List<ProjectActivity>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Activity>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfActivitiesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /Activities/count");

    internal static ProjectActivity Map(Raw.Activity source) => new()
    {
        Number = source.Number,
        GroupNumber = source.GroupNumber,
        Name = source.Name,
        IsBarred = source.IsBarred,
        InLieuCode = source.InLieuCode,
        HideInSearch = source.HideInSearch,
        CutoffDate = source.CutoffDate,
        SalesPriceBefore = source.SalesPriceBefore,
        SalesPriceAfter = source.SalesPriceAfter,
        CostPriceMarkupPercentage = source.CostPriceMarkupPercentage,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.Activity ToGenerated(ProjectActivity source) => new()
    {
        Number = source.Number,
        GroupNumber = source.GroupNumber,
        Name = source.Name,
        IsBarred = source.IsBarred,
        InLieuCode = source.InLieuCode,
        HideInSearch = source.HideInSearch,
        CutoffDate = source.CutoffDate,
        SalesPriceBefore = source.SalesPriceBefore,
        SalesPriceAfter = source.SalesPriceAfter,
        CostPriceMarkupPercentage = source.CostPriceMarkupPercentage,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/Activities</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectActivityResource
{
    private readonly Raw.ActivitiesClient _client;

    internal ProjectActivityResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ActivitiesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectActivity, ProjectActivityFilter, ProjectActivitySort> AsQuery() => new(new ProjectActivitySource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectActivity, ProjectActivityFilter, ProjectActivitySort> Where(System.Linq.Expressions.Expression<System.Func<ProjectActivityFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectActivity, ProjectActivityFilter, ProjectActivitySort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectActivity, ProjectActivityFilter, ProjectActivitySort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectActivitySort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectActivity, ProjectActivityFilter, ProjectActivitySort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectActivitySort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectActivity, ProjectActivityFilter, ProjectActivitySort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectActivity> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectActivity>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectActivity> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetActivityByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /Activities/{number}").ConfigureAwait(false);

        return ProjectActivitySource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectActivity item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateActivityAsync(body: ProjectActivitySource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /Activities").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectActivity item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateActivityByIdAsync(body: ProjectActivitySource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /Activities");
    }
}

/// <summary>
/// A resource from <c>/ActivityGroups</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectActivityGroup
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>type</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int Type { get; init; }

    /// <summary>The <c>includeCostPriceInFinance</c> field.</summary>
    public bool? IncludeCostPriceInFinance { get; init; }

    /// <summary>The <c>costAccountClosed</c> field.</summary>
    public int? CostAccountClosed { get; init; }

    /// <summary>The <c>costAccountOngoing</c> field.</summary>
    public int? CostAccountOngoing { get; init; }

    /// <summary>The <c>costAccountOngoingType</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int CostAccountOngoingType { get; init; }

    /// <summary>The <c>costContraAccountOngoing</c> field.</summary>
    public int? CostContraAccountOngoing { get; init; }

    /// <summary>The <c>includeSalesPriceInFinance</c> field.</summary>
    public bool? IncludeSalesPriceInFinance { get; init; }

    /// <summary>The <c>salesAccountClosed</c> field.</summary>
    public int? SalesAccountClosed { get; init; }

    /// <summary>The <c>salesAccountOngoing</c> field.</summary>
    public int? SalesAccountOngoing { get; init; }

    /// <summary>The <c>salesAccountOngoingType</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int SalesAccountOngoingType { get; init; }

    /// <summary>The <c>salesContraAccountOngoing</c> field.</summary>
    public int? SalesContraAccountOngoing { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/ActivityGroups</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectActivityGroupFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>type</c>.</summary>
    [EconomicField("type")]
    public EqualityField<int> Type { get; } = null!;

    /// <summary>Maps to <c>includeCostPriceInFinance</c>.</summary>
    [EconomicField("includeCostPriceInFinance")]
    public EqualityField<bool> IncludeCostPriceInFinance { get; } = null!;

    /// <summary>Maps to <c>costAccountClosed</c>.</summary>
    [EconomicField("costAccountClosed")]
    public NumericField<int> CostAccountClosed { get; } = null!;

    /// <summary>Maps to <c>costAccountOngoing</c>.</summary>
    [EconomicField("costAccountOngoing")]
    public NumericField<int> CostAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>costContraAccountOngoing</c>.</summary>
    [EconomicField("costContraAccountOngoing")]
    public NumericField<int> CostContraAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>includeSalesPriceInFinance</c>.</summary>
    [EconomicField("includeSalesPriceInFinance")]
    public EqualityField<bool> IncludeSalesPriceInFinance { get; } = null!;

    /// <summary>Maps to <c>salesAccountClosed</c>.</summary>
    [EconomicField("salesAccountClosed")]
    public NumericField<int> SalesAccountClosed { get; } = null!;

    /// <summary>Maps to <c>salesAccountOngoing</c>.</summary>
    [EconomicField("salesAccountOngoing")]
    public NumericField<int> SalesAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>salesContraAccountOngoing</c>.</summary>
    [EconomicField("salesContraAccountOngoing")]
    public NumericField<int> SalesContraAccountOngoing { get; } = null!;
}

/// <summary>
/// What <c>/ActivityGroups</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectActivityGroupSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public EconomicSortField Name { get; } = null!;

    /// <summary>Maps to <c>type</c>.</summary>
    [EconomicField("type")]
    public EconomicSortField Type { get; } = null!;
}

/// <summary>Fetches <c>/ActivityGroups</c> and maps it to <see cref="ProjectActivityGroup"/>.</summary>
internal sealed class ProjectActivityGroupSource(Raw.ActivityGroupsClient client)
    : IEconomicOpenSource<ProjectActivityGroup>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectActivityGroup>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllActivityGroupsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /ActivityGroups").ConfigureAwait(false);

        var items = new List<ProjectActivityGroup>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectActivityGroup>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectActivityGroup>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfActivityGroupsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /ActivityGroups/paged").ConfigureAwait(false);

        var items = new List<ProjectActivityGroup>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.ActivityGroup>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfActivityGroupsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /ActivityGroups/count");

    internal static ProjectActivityGroup Map(Raw.ActivityGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (int)source.Type,
        IncludeCostPriceInFinance = source.IncludeCostPriceInFinance,
        CostAccountClosed = source.CostAccountClosed,
        CostAccountOngoing = source.CostAccountOngoing,
        CostAccountOngoingType = (int)source.CostAccountOngoingType,
        CostContraAccountOngoing = source.CostContraAccountOngoing,
        IncludeSalesPriceInFinance = source.IncludeSalesPriceInFinance,
        SalesAccountClosed = source.SalesAccountClosed,
        SalesAccountOngoing = source.SalesAccountOngoing,
        SalesAccountOngoingType = (int)source.SalesAccountOngoingType,
        SalesContraAccountOngoing = source.SalesContraAccountOngoing,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.ActivityGroup ToGenerated(ProjectActivityGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (Raw.ActivityGroupType)source.Type,
        IncludeCostPriceInFinance = source.IncludeCostPriceInFinance,
        CostAccountClosed = source.CostAccountClosed,
        CostAccountOngoing = source.CostAccountOngoing,
        CostAccountOngoingType = (Raw.OngoingAccountType)source.CostAccountOngoingType,
        CostContraAccountOngoing = source.CostContraAccountOngoing,
        IncludeSalesPriceInFinance = source.IncludeSalesPriceInFinance,
        SalesAccountClosed = source.SalesAccountClosed,
        SalesAccountOngoing = source.SalesAccountOngoing,
        SalesAccountOngoingType = (Raw.OngoingAccountType)source.SalesAccountOngoingType,
        SalesContraAccountOngoing = source.SalesContraAccountOngoing,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/ActivityGroups</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectActivityGroupResource
{
    private readonly Raw.ActivityGroupsClient _client;

    internal ProjectActivityGroupResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ActivityGroupsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectActivityGroup, ProjectActivityGroupFilter, ProjectActivityGroupSort> AsQuery() => new(new ProjectActivityGroupSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectActivityGroup, ProjectActivityGroupFilter, ProjectActivityGroupSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectActivityGroupFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectActivityGroup, ProjectActivityGroupFilter, ProjectActivityGroupSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectActivityGroup, ProjectActivityGroupFilter, ProjectActivityGroupSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectActivityGroupSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectActivityGroup, ProjectActivityGroupFilter, ProjectActivityGroupSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectActivityGroupSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectActivityGroup, ProjectActivityGroupFilter, ProjectActivityGroupSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectActivityGroup> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectActivityGroup>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectActivityGroup> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetActivityGroupByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /ActivityGroups/{number}").ConfigureAwait(false);

        return ProjectActivityGroupSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectActivityGroup item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateActivityGroupAsync(body: ProjectActivityGroupSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /ActivityGroups").ConfigureAwait(false);

        return response.Number;
    }
}

/// <summary>
/// A resource from <c>/CostTypes</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectCostType
{
    /// <summary>The <c>number</c> field.</summary>
    public required int Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>costGroupNumber</c> field.</summary>
    public required int CostGroupNumber { get; init; }

    /// <summary>The <c>vatCode</c> field.</summary>
    public string? VatCode { get; init; }

    /// <summary>The <c>isBarred</c> field.</summary>
    public bool? IsBarred { get; init; }

    /// <summary>The <c>lastUpdated</c> field.</summary>
    public System.DateTimeOffset? LastUpdated { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/CostTypes</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectCostTypeFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>isBarred</c>.</summary>
    [EconomicField("isBarred")]
    public EqualityField<bool> IsBarred { get; } = null!;

    /// <summary>Maps to <c>lastUpdated</c>.</summary>
    [EconomicField("lastUpdated")]
    public ComparableField<System.DateTimeOffset> LastUpdated { get; } = null!;
}

/// <summary>
/// What <c>/CostTypes</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectCostTypeSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public EconomicSortField Name { get; } = null!;

    /// <summary>Maps to <c>costGroupNumber</c>.</summary>
    [EconomicField("costGroupNumber")]
    public EconomicSortField CostGroupNumber { get; } = null!;

    /// <summary>Maps to <c>vatCode</c>.</summary>
    [EconomicField("vatCode")]
    public EconomicSortField VatCode { get; } = null!;
}

/// <summary>Fetches <c>/CostTypes</c> and maps it to <see cref="ProjectCostType"/>.</summary>
internal sealed class ProjectCostTypeSource(Raw.CostTypesClient client)
    : IEconomicOpenSource<ProjectCostType>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectCostType>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllCostTypesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /CostTypes").ConfigureAwait(false);

        var items = new List<ProjectCostType>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectCostType>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectCostType>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfCostTypesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /CostTypes/paged").ConfigureAwait(false);

        var items = new List<ProjectCostType>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.CostType>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfCostTypesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /CostTypes/count");

    internal static ProjectCostType Map(Raw.CostType source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        CostGroupNumber = source.CostGroupNumber,
        VatCode = source.VatCode,
        IsBarred = source.IsBarred,
        LastUpdated = source.LastUpdated,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.CostType ToGenerated(ProjectCostType source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        CostGroupNumber = source.CostGroupNumber,
        VatCode = source.VatCode,
        IsBarred = source.IsBarred,
        LastUpdated = source.LastUpdated,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/CostTypes</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectCostTypeResource
{
    private readonly Raw.CostTypesClient _client;

    internal ProjectCostTypeResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.CostTypesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectCostType, ProjectCostTypeFilter, ProjectCostTypeSort> AsQuery() => new(new ProjectCostTypeSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectCostType, ProjectCostTypeFilter, ProjectCostTypeSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectCostTypeFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectCostType, ProjectCostTypeFilter, ProjectCostTypeSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectCostType, ProjectCostTypeFilter, ProjectCostTypeSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectCostTypeSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectCostType, ProjectCostTypeFilter, ProjectCostTypeSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectCostTypeSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectCostType, ProjectCostTypeFilter, ProjectCostTypeSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectCostType> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectCostType>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectCostType> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetCostTypeByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /CostTypes/{number}").ConfigureAwait(false);

        return ProjectCostTypeSource.Map(response);
    }
}

/// <summary>
/// A resource from <c>/CostTypeGroups</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectCostTypeGroup
{
    /// <summary>The <c>number</c> field.</summary>
    public required int Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>type</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int Type { get; init; }

    /// <summary>The <c>markup</c> field.</summary>
    public double? Markup { get; init; }

    /// <summary>The <c>accountOnGoing</c> field.</summary>
    public required int AccountOnGoing { get; init; }

    /// <summary>The <c>accountClosed</c> field.</summary>
    public required int AccountClosed { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/CostTypeGroups</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectCostTypeGroupFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;
}

/// <summary>
/// What <c>/CostTypeGroups</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectCostTypeGroupSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public EconomicSortField Name { get; } = null!;

    /// <summary>Maps to <c>markup</c>.</summary>
    [EconomicField("markup")]
    public EconomicSortField Markup { get; } = null!;

    /// <summary>Maps to <c>accountOnGoing</c>.</summary>
    [EconomicField("accountOnGoing")]
    public EconomicSortField AccountOnGoing { get; } = null!;

    /// <summary>Maps to <c>accountClosed</c>.</summary>
    [EconomicField("accountClosed")]
    public EconomicSortField AccountClosed { get; } = null!;
}

/// <summary>Fetches <c>/CostTypeGroups</c> and maps it to <see cref="ProjectCostTypeGroup"/>.</summary>
internal sealed class ProjectCostTypeGroupSource(Raw.CostTypeGroupsClient client)
    : IEconomicOpenSource<ProjectCostTypeGroup>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectCostTypeGroup>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllCostTypeGroupsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /CostTypeGroups").ConfigureAwait(false);

        var items = new List<ProjectCostTypeGroup>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectCostTypeGroup>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectCostTypeGroup>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfCostTypeGroupsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /CostTypeGroups/paged").ConfigureAwait(false);

        var items = new List<ProjectCostTypeGroup>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.CostTypeGroup>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfCostTypeGroupsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /CostTypeGroups/count");

    internal static ProjectCostTypeGroup Map(Raw.CostTypeGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (int)source.Type,
        Markup = source.Markup,
        AccountOnGoing = source.AccountOnGoing,
        AccountClosed = source.AccountClosed,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.CostTypeGroup ToGenerated(ProjectCostTypeGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (Raw.CostGroupType)source.Type,
        Markup = source.Markup,
        AccountOnGoing = source.AccountOnGoing,
        AccountClosed = source.AccountClosed,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/CostTypeGroups</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectCostTypeGroupResource
{
    private readonly Raw.CostTypeGroupsClient _client;

    internal ProjectCostTypeGroupResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.CostTypeGroupsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectCostTypeGroup, ProjectCostTypeGroupFilter, ProjectCostTypeGroupSort> AsQuery() => new(new ProjectCostTypeGroupSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectCostTypeGroup, ProjectCostTypeGroupFilter, ProjectCostTypeGroupSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectCostTypeGroupFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectCostTypeGroup, ProjectCostTypeGroupFilter, ProjectCostTypeGroupSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectCostTypeGroup, ProjectCostTypeGroupFilter, ProjectCostTypeGroupSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectCostTypeGroupSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectCostTypeGroup, ProjectCostTypeGroupFilter, ProjectCostTypeGroupSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectCostTypeGroupSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectCostTypeGroup, ProjectCostTypeGroupFilter, ProjectCostTypeGroupSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectCostTypeGroup> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectCostTypeGroup>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectCostTypeGroup> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetCostTypeGroupByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /CostTypeGroups/{number}").ConfigureAwait(false);

        return ProjectCostTypeGroupSource.Map(response);
    }
}

/// <summary>
/// A resource from <c>/Employees</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectEmployee
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>groupNumber</c> field.</summary>
    public required int GroupNumber { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>isBarred</c> field.</summary>
    public required bool IsBarred { get; init; }

    /// <summary>The <c>phone</c> field.</summary>
    public string? Phone { get; init; }

    /// <summary>The <c>email</c> field.</summary>
    public string? Email { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/Employees</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectEmployeeFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>groupNumber</c>.</summary>
    [EconomicField("groupNumber")]
    public NumericField<int> GroupNumber { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>isBarred</c>.</summary>
    [EconomicField("isBarred")]
    public EqualityField<bool> IsBarred { get; } = null!;

    /// <summary>Maps to <c>phone</c>.</summary>
    [EconomicField("phone")]
    public EqualityField<string> Phone { get; } = null!;

    /// <summary>Maps to <c>email</c>.</summary>
    [EconomicField("email")]
    public EqualityField<string> Email { get; } = null!;
}

/// <summary>
/// What <c>/Employees</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectEmployeeSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/Employees</c> and maps it to <see cref="ProjectEmployee"/>.</summary>
internal sealed class ProjectEmployeeSource(Raw.EmployeesClient client)
    : IEconomicOpenSource<ProjectEmployee>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectEmployee>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllEmployeesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /Employees").ConfigureAwait(false);

        var items = new List<ProjectEmployee>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectEmployee>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectEmployee>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfEmployeesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /Employees/paged").ConfigureAwait(false);

        var items = new List<ProjectEmployee>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Employee>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfEmployeesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /Employees/count");

    internal static ProjectEmployee Map(Raw.Employee source) => new()
    {
        Number = source.Number,
        GroupNumber = source.GroupNumber,
        Name = source.Name,
        IsBarred = source.IsBarred,
        Phone = source.Phone,
        Email = source.Email,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.Employee ToGenerated(ProjectEmployee source) => new()
    {
        Number = source.Number,
        GroupNumber = source.GroupNumber,
        Name = source.Name,
        IsBarred = source.IsBarred,
        Phone = source.Phone,
        Email = source.Email,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/Employees</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectEmployeeResource
{
    private readonly Raw.EmployeesClient _client;

    internal ProjectEmployeeResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.EmployeesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectEmployee, ProjectEmployeeFilter, ProjectEmployeeSort> AsQuery() => new(new ProjectEmployeeSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectEmployee, ProjectEmployeeFilter, ProjectEmployeeSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectEmployee, ProjectEmployeeFilter, ProjectEmployeeSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectEmployee, ProjectEmployeeFilter, ProjectEmployeeSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectEmployee, ProjectEmployeeFilter, ProjectEmployeeSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectEmployee, ProjectEmployeeFilter, ProjectEmployeeSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectEmployee> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectEmployee>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectEmployee> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetEmployeeByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /Employees/{number}").ConfigureAwait(false);

        return ProjectEmployeeSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectEmployee item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateEmployeeAsync(body: ProjectEmployeeSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /Employees").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectEmployee item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateEmployeeByIdAsync(body: ProjectEmployeeSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /Employees");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteEmployeeByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /Employees/{number}");
}

/// <summary>
/// A resource from <c>/EmployeeGroups</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectEmployeeGroup
{
    /// <summary>The <c>number</c> field.</summary>
    public required int Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/EmployeeGroups</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectEmployeeGroupFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;
}

/// <summary>
/// What <c>/EmployeeGroups</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectEmployeeGroupSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/EmployeeGroups</c> and maps it to <see cref="ProjectEmployeeGroup"/>.</summary>
internal sealed class ProjectEmployeeGroupSource(Raw.EmployeeGroupsClient client)
    : IEconomicOpenSource<ProjectEmployeeGroup>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectEmployeeGroup>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllEmployeeGroupsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /EmployeeGroups").ConfigureAwait(false);

        var items = new List<ProjectEmployeeGroup>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectEmployeeGroup>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectEmployeeGroup>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfEmployeeGroupsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /EmployeeGroups/paged").ConfigureAwait(false);

        var items = new List<ProjectEmployeeGroup>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.EmployeeGroup>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfEmployeeGroupsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /EmployeeGroups/count");

    internal static ProjectEmployeeGroup Map(Raw.EmployeeGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.EmployeeGroup ToGenerated(ProjectEmployeeGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/EmployeeGroups</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectEmployeeGroupResource
{
    private readonly Raw.EmployeeGroupsClient _client;

    internal ProjectEmployeeGroupResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.EmployeeGroupsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectEmployeeGroup, ProjectEmployeeGroupFilter, ProjectEmployeeGroupSort> AsQuery() => new(new ProjectEmployeeGroupSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectEmployeeGroup, ProjectEmployeeGroupFilter, ProjectEmployeeGroupSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeGroupFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectEmployeeGroup, ProjectEmployeeGroupFilter, ProjectEmployeeGroupSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectEmployeeGroup, ProjectEmployeeGroupFilter, ProjectEmployeeGroupSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeGroupSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectEmployeeGroup, ProjectEmployeeGroupFilter, ProjectEmployeeGroupSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeGroupSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectEmployeeGroup, ProjectEmployeeGroupFilter, ProjectEmployeeGroupSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectEmployeeGroup> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectEmployeeGroup>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectEmployeeGroup> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetEmployeeGroupByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /EmployeeGroups/{number}").ConfigureAwait(false);

        return ProjectEmployeeGroupSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectEmployeeGroup item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateEmployeeGroupAsync(body: ProjectEmployeeGroupSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /EmployeeGroups").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectEmployeeGroup item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateEmployeeGroupByIdAsync(body: ProjectEmployeeGroupSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /EmployeeGroups");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteEmployeeGroupByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /EmployeeGroups/{number}");
}

/// <summary>
/// A resource from <c>/Mileages</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectMileage
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>projectNumber</c> field.</summary>
    public required int ProjectNumber { get; init; }

    /// <summary>The <c>employeeNumber</c> field.</summary>
    public required int EmployeeNumber { get; init; }

    /// <summary>The <c>date</c> field.</summary>
    public required System.DateTimeOffset Date { get; init; }

    /// <summary>The <c>distance</c> field.</summary>
    public required double Distance { get; init; }

    /// <summary>The <c>from</c> field.</summary>
    public string? From { get; init; }

    /// <summary>The <c>to</c> field.</summary>
    public string? To { get; init; }

    /// <summary>The <c>isApproved</c> field.</summary>
    public bool? IsApproved { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/Mileages</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectMileageFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>projectNumber</c>.</summary>
    [EconomicField("projectNumber")]
    public NumericField<int> ProjectNumber { get; } = null!;

    /// <summary>Maps to <c>employeeNumber</c>.</summary>
    [EconomicField("employeeNumber")]
    public NumericField<int> EmployeeNumber { get; } = null!;

    /// <summary>Maps to <c>date</c>.</summary>
    [EconomicField("date")]
    public ComparableField<System.DateTimeOffset> Date { get; } = null!;

    /// <summary>Maps to <c>isApproved</c>.</summary>
    [EconomicField("isApproved")]
    public EqualityField<bool> IsApproved { get; } = null!;
}

/// <summary>
/// What <c>/Mileages</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectMileageSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/Mileages</c> and maps it to <see cref="ProjectMileage"/>.</summary>
internal sealed class ProjectMileageSource(Raw.MileagesClient client)
    : IEconomicOpenSource<ProjectMileage>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectMileage>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllMileagesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /Mileages").ConfigureAwait(false);

        var items = new List<ProjectMileage>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectMileage>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectMileage>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfMileagesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /Mileages/paged").ConfigureAwait(false);

        var items = new List<ProjectMileage>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Mileage>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfMileagesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /Mileages/count");

    internal static ProjectMileage Map(Raw.Mileage source) => new()
    {
        Number = source.Number,
        ProjectNumber = source.ProjectNumber,
        EmployeeNumber = source.EmployeeNumber,
        Date = source.Date,
        Distance = source.Distance,
        From = source.From,
        To = source.To,
        IsApproved = source.IsApproved,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.Mileage ToGenerated(ProjectMileage source) => new()
    {
        Number = source.Number,
        ProjectNumber = source.ProjectNumber,
        EmployeeNumber = source.EmployeeNumber,
        Date = source.Date,
        Distance = source.Distance,
        From = source.From,
        To = source.To,
        IsApproved = source.IsApproved,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/Mileages</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectMileageResource
{
    private readonly Raw.MileagesClient _client;

    internal ProjectMileageResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.MileagesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectMileage, ProjectMileageFilter, ProjectMileageSort> AsQuery() => new(new ProjectMileageSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectMileage, ProjectMileageFilter, ProjectMileageSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectMileageFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectMileage, ProjectMileageFilter, ProjectMileageSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectMileage, ProjectMileageFilter, ProjectMileageSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectMileageSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectMileage, ProjectMileageFilter, ProjectMileageSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectMileageSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectMileage, ProjectMileageFilter, ProjectMileageSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectMileage> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectMileage>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectMileage> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetMileageByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /Mileages/{number}").ConfigureAwait(false);

        return ProjectMileageSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectMileage item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateMileageAsync(body: ProjectMileageSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /Mileages").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectMileage item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateMileageByIdAsync(body: ProjectMileageSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /Mileages");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteMileageByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /Mileages/{number}");
}

/// <summary>
/// A resource from <c>/MileagePrices</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectMileagePrice
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>costPrice</c> field.</summary>
    public double? CostPrice { get; init; }

    /// <summary>The <c>costRate</c> field.</summary>
    public double? CostRate { get; init; }

    /// <summary>The <c>salesPrice</c> field.</summary>
    public double? SalesPrice { get; init; }

    /// <summary>The <c>salesRate</c> field.</summary>
    public double? SalesRate { get; init; }
}

/// <summary>
/// What <c>/MileagePrices</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectMileagePriceFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;
}

/// <summary>
/// What <c>/MileagePrices</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectMileagePriceSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/MileagePrices</c> and maps it to <see cref="ProjectMileagePrice"/>.</summary>
internal sealed class ProjectMileagePriceSource(Raw.MileagePricesClient client)
    : IEconomicOpenSource<ProjectMileagePrice>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectMileagePrice>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllMileagePricesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /MileagePrices").ConfigureAwait(false);

        var items = new List<ProjectMileagePrice>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectMileagePrice>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectMileagePrice>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfMileagePricesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /MileagePrices/paged").ConfigureAwait(false);

        var items = new List<ProjectMileagePrice>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.MileagePrices>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        throw new NotSupportedException(
            "e-conomic publishes no count endpoint for /MileagePrices, so this collection "
            + "cannot be counted. Enumerate it, or count a collection that publishes one.");

    internal static ProjectMileagePrice Map(Raw.MileagePrices source) => new()
    {
        Number = source.Number,
        CostPrice = source.CostPrice,
        CostRate = source.CostRate,
        SalesPrice = source.SalesPrice,
        SalesRate = source.SalesRate,
    };

    internal static Raw.MileagePrices ToGenerated(ProjectMileagePrice source) => new()
    {
        Number = source.Number,
        CostPrice = source.CostPrice,
        CostRate = source.CostRate,
        SalesPrice = source.SalesPrice,
        SalesRate = source.SalesRate,
    };
}

/// <summary>
/// <c>/MileagePrices</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectMileagePriceResource
{
    private readonly Raw.MileagePricesClient _client;

    internal ProjectMileagePriceResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.MileagePricesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectMileagePrice, ProjectMileagePriceFilter, ProjectMileagePriceSort> AsQuery() => new(new ProjectMileagePriceSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectMileagePrice, ProjectMileagePriceFilter, ProjectMileagePriceSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectMileagePriceFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectMileagePrice, ProjectMileagePriceFilter, ProjectMileagePriceSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectMileagePrice, ProjectMileagePriceFilter, ProjectMileagePriceSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectMileagePriceSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectMileagePrice, ProjectMileagePriceFilter, ProjectMileagePriceSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectMileagePriceSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectMileagePrice, ProjectMileagePriceFilter, ProjectMileagePriceSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectMileagePrice> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectMileagePrice>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectMileagePrice> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetMileagePricesByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /MileagePrices/{number}").ConfigureAwait(false);

        return ProjectMileagePriceSource.Map(response);
    }
}

/// <summary>
/// A resource from <c>/Projects</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record Project
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>projectGroupNumber</c> field.</summary>
    public required int ProjectGroupNumber { get; init; }

    /// <summary>The <c>customerNumber</c> field.</summary>
    public int? CustomerNumber { get; init; }

    /// <summary>The <c>isClosed</c> field.</summary>
    public bool? IsClosed { get; init; }

    /// <summary>The <c>mileage</c> field.</summary>
    public double? Mileage { get; init; }

    /// <summary>The <c>isBarred</c> field.</summary>
    public bool? IsBarred { get; init; }

    /// <summary>The <c>isMainProject</c> field.</summary>
    public bool? IsMainProject { get; init; }

    /// <summary>The <c>isMileageInvoiced</c> field.</summary>
    public bool? IsMileageInvoiced { get; init; }

    /// <summary>The <c>lastUpdated</c> field.</summary>
    public System.DateTimeOffset? LastUpdated { get; init; }

    /// <summary>The <c>responsibleEmployeeNumber</c> field.</summary>
    public int? ResponsibleEmployeeNumber { get; init; }

    /// <summary>The <c>mainProjectNumber</c> field.</summary>
    public int? MainProjectNumber { get; init; }

    /// <summary>The <c>otherResponsibleEmployeeNumber</c> field.</summary>
    public int? OtherResponsibleEmployeeNumber { get; init; }

    /// <summary>The <c>status</c> field.</summary>
    public int? Status { get; init; }

    /// <summary>The <c>description</c> field.</summary>
    public string? Description { get; init; }

    /// <summary>The <c>deliveryDate</c> field.</summary>
    public System.DateTimeOffset? DeliveryDate { get; init; }

    /// <summary>The <c>departmentNumber</c> field.</summary>
    public int? DepartmentNumber { get; init; }

    /// <summary>The <c>closedDate</c> field.</summary>
    public System.DateTimeOffset? ClosedDate { get; init; }

    /// <summary>The <c>fixedPrice</c> field.</summary>
    public double? FixedPrice { get; init; }

    /// <summary>The <c>costPrice</c> field.</summary>
    public double? CostPrice { get; init; }

    /// <summary>The <c>salesPrice</c> field.</summary>
    public double? SalesPrice { get; init; }

    /// <summary>The <c>invoicedTotal</c> field.</summary>
    public double? InvoicedTotal { get; init; }

    /// <summary>The <c>contactPersonId</c> field.</summary>
    public int? ContactPersonId { get; init; }

    /// <summary>The <c>deliveryLocationNumber</c> field.</summary>
    public int? DeliveryLocationNumber { get; init; }

    /// <summary>The <c>otherReference</c> field.</summary>
    public string? OtherReference { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/Projects</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>projectGroupNumber</c>.</summary>
    [EconomicField("projectGroupNumber")]
    public NumericField<int> ProjectGroupNumber { get; } = null!;

    /// <summary>Maps to <c>customerNumber</c>.</summary>
    [EconomicField("customerNumber")]
    public NumericField<int> CustomerNumber { get; } = null!;

    /// <summary>Maps to <c>isClosed</c>.</summary>
    [EconomicField("isClosed")]
    public EqualityField<bool> IsClosed { get; } = null!;

    /// <summary>Maps to <c>mileage</c>.</summary>
    [EconomicField("mileage")]
    public NumericField<double> Mileage { get; } = null!;

    /// <summary>Maps to <c>isBarred</c>.</summary>
    [EconomicField("isBarred")]
    public EqualityField<bool> IsBarred { get; } = null!;

    /// <summary>Maps to <c>isMainProject</c>.</summary>
    [EconomicField("isMainProject")]
    public EqualityField<bool> IsMainProject { get; } = null!;

    /// <summary>Maps to <c>isMileageInvoiced</c>.</summary>
    [EconomicField("isMileageInvoiced")]
    public EqualityField<bool> IsMileageInvoiced { get; } = null!;

    /// <summary>Maps to <c>lastUpdated</c>.</summary>
    [EconomicField("lastUpdated")]
    public ComparableField<System.DateTimeOffset> LastUpdated { get; } = null!;

    /// <summary>Maps to <c>responsibleEmployeeNumber</c>.</summary>
    [EconomicField("responsibleEmployeeNumber")]
    public NumericField<int> ResponsibleEmployeeNumber { get; } = null!;

    /// <summary>Maps to <c>mainProjectNumber</c>.</summary>
    [EconomicField("mainProjectNumber")]
    public NumericField<int> MainProjectNumber { get; } = null!;

    /// <summary>Maps to <c>otherResponsibleEmployeeNumber</c>.</summary>
    [EconomicField("otherResponsibleEmployeeNumber")]
    public NumericField<int> OtherResponsibleEmployeeNumber { get; } = null!;

    /// <summary>Maps to <c>status</c>.</summary>
    [EconomicField("status")]
    public NumericField<int> Status { get; } = null!;

    /// <summary>Maps to <c>deliveryDate</c>.</summary>
    [EconomicField("deliveryDate")]
    public ComparableField<System.DateTimeOffset> DeliveryDate { get; } = null!;

    /// <summary>Maps to <c>departmentNumber</c>.</summary>
    [EconomicField("departmentNumber")]
    public NumericField<int> DepartmentNumber { get; } = null!;

    /// <summary>Maps to <c>closedDate</c>.</summary>
    [EconomicField("closedDate")]
    public ComparableField<System.DateTimeOffset> ClosedDate { get; } = null!;

    /// <summary>Maps to <c>fixedPrice</c>.</summary>
    [EconomicField("fixedPrice")]
    public NumericField<double> FixedPrice { get; } = null!;

    /// <summary>Maps to <c>costPrice</c>.</summary>
    [EconomicField("costPrice")]
    public NumericField<double> CostPrice { get; } = null!;

    /// <summary>Maps to <c>salesPrice</c>.</summary>
    [EconomicField("salesPrice")]
    public NumericField<double> SalesPrice { get; } = null!;

    /// <summary>Maps to <c>invoicedTotal</c>.</summary>
    [EconomicField("invoicedTotal")]
    public NumericField<double> InvoicedTotal { get; } = null!;

    /// <summary>Maps to <c>contactPersonId</c>.</summary>
    [EconomicField("contactPersonId")]
    public NumericField<int> ContactPersonId { get; } = null!;

    /// <summary>Maps to <c>deliveryLocationNumber</c>.</summary>
    [EconomicField("deliveryLocationNumber")]
    public NumericField<int> DeliveryLocationNumber { get; } = null!;

    /// <summary>Maps to <c>otherReference</c>.</summary>
    [EconomicField("otherReference")]
    public EqualityField<string> OtherReference { get; } = null!;
}

/// <summary>
/// What <c>/Projects</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/Projects</c> and maps it to <see cref="Project"/>.</summary>
internal sealed class ProjectSource(Raw.ProjectsClient client)
    : IEconomicOpenSource<Project>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<Project>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllProjectsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /Projects").ConfigureAwait(false);

        var items = new List<Project>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<Project>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<Project>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfProjectsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /Projects/paged").ConfigureAwait(false);

        var items = new List<Project>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Project>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfProjectsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /Projects/count");

    internal static Project Map(Raw.Project source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        ProjectGroupNumber = source.ProjectGroupNumber,
        CustomerNumber = source.CustomerNumber,
        IsClosed = source.IsClosed,
        Mileage = source.Mileage,
        IsBarred = source.IsBarred,
        IsMainProject = source.IsMainProject,
        IsMileageInvoiced = source.IsMileageInvoiced,
        LastUpdated = source.LastUpdated,
        ResponsibleEmployeeNumber = source.ResponsibleEmployeeNumber,
        MainProjectNumber = source.MainProjectNumber,
        OtherResponsibleEmployeeNumber = source.OtherResponsibleEmployeeNumber,
        Status = source.Status,
        Description = source.Description,
        DeliveryDate = source.DeliveryDate,
        DepartmentNumber = source.DepartmentNumber,
        ClosedDate = source.ClosedDate,
        FixedPrice = source.FixedPrice,
        CostPrice = source.CostPrice,
        SalesPrice = source.SalesPrice,
        InvoicedTotal = source.InvoicedTotal,
        ContactPersonId = source.ContactPersonId,
        DeliveryLocationNumber = source.DeliveryLocationNumber,
        OtherReference = source.OtherReference,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.Project ToGenerated(Project source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        ProjectGroupNumber = source.ProjectGroupNumber,
        CustomerNumber = source.CustomerNumber,
        IsClosed = source.IsClosed,
        Mileage = source.Mileage,
        IsBarred = source.IsBarred,
        IsMainProject = source.IsMainProject,
        IsMileageInvoiced = source.IsMileageInvoiced,
        LastUpdated = source.LastUpdated,
        ResponsibleEmployeeNumber = source.ResponsibleEmployeeNumber,
        MainProjectNumber = source.MainProjectNumber,
        OtherResponsibleEmployeeNumber = source.OtherResponsibleEmployeeNumber,
        Status = source.Status,
        Description = source.Description,
        DeliveryDate = source.DeliveryDate,
        DepartmentNumber = source.DepartmentNumber,
        ClosedDate = source.ClosedDate,
        FixedPrice = source.FixedPrice,
        CostPrice = source.CostPrice,
        SalesPrice = source.SalesPrice,
        InvoicedTotal = source.InvoicedTotal,
        ContactPersonId = source.ContactPersonId,
        DeliveryLocationNumber = source.DeliveryLocationNumber,
        OtherReference = source.OtherReference,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/Projects</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectResource
{
    private readonly Raw.ProjectsClient _client;

    internal ProjectResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ProjectsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<Project, ProjectFilter, ProjectSort> AsQuery() => new(new ProjectSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<Project, ProjectFilter, ProjectSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<Project, ProjectFilter, ProjectSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<Project, ProjectFilter, ProjectSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<Project, ProjectFilter, ProjectSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<Project, ProjectFilter, ProjectSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<Project> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<Project>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<Project> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetProjectByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /Projects/{number}").ConfigureAwait(false);

        return ProjectSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(Project item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateProjectAsync(body: ProjectSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /Projects").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(Project item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateProjectByIdAsync(body: ProjectSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /Projects");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteProjectByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /Projects/{number}");
}

/// <summary>
/// A resource from <c>/project-activities</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectActivityAssignment
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>projectNumber</c> field.</summary>
    public required int ProjectNumber { get; init; }

    /// <summary>The <c>activityNumber</c> field.</summary>
    public required int ActivityNumber { get; init; }

    /// <summary>The <c>startDate</c> field.</summary>
    public System.DateTimeOffset? StartDate { get; init; }

    /// <summary>The <c>endDate</c> field.</summary>
    public System.DateTimeOffset? EndDate { get; init; }

    /// <summary>The <c>responsibleEmployeeNumber</c> field.</summary>
    public int? ResponsibleEmployeeNumber { get; init; }

    /// <summary>The <c>completed</c> field.</summary>
    public bool? Completed { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>lastUpdated</c> field.</summary>
    public System.DateTimeOffset? LastUpdated { get; init; }
}

/// <summary>
/// What <c>/project-activities</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectActivityAssignmentFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>projectNumber</c>.</summary>
    [EconomicField("projectNumber")]
    public NumericField<int> ProjectNumber { get; } = null!;

    /// <summary>Maps to <c>activityNumber</c>.</summary>
    [EconomicField("activityNumber")]
    public NumericField<int> ActivityNumber { get; } = null!;

    /// <summary>Maps to <c>startDate</c>.</summary>
    [EconomicField("startDate")]
    public ComparableField<System.DateTimeOffset> StartDate { get; } = null!;

    /// <summary>Maps to <c>endDate</c>.</summary>
    [EconomicField("endDate")]
    public ComparableField<System.DateTimeOffset> EndDate { get; } = null!;

    /// <summary>Maps to <c>responsibleEmployeeNumber</c>.</summary>
    [EconomicField("responsibleEmployeeNumber")]
    public NumericField<int> ResponsibleEmployeeNumber { get; } = null!;

    /// <summary>Maps to <c>completed</c>.</summary>
    [EconomicField("completed")]
    public EqualityField<bool> Completed { get; } = null!;

    /// <summary>Maps to <c>lastUpdated</c>.</summary>
    [EconomicField("lastUpdated")]
    public ComparableField<System.DateTimeOffset> LastUpdated { get; } = null!;
}

/// <summary>
/// What <c>/project-activities</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectActivityAssignmentSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/project-activities</c> and maps it to <see cref="ProjectActivityAssignment"/>.</summary>
internal sealed class ProjectActivityAssignmentSource(Raw.ProjectActivitiesClient client)
    : IEconomicOpenSource<ProjectActivityAssignment>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectActivityAssignment>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllProjectActivitiesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /project-activities").ConfigureAwait(false);

        var items = new List<ProjectActivityAssignment>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectActivityAssignment>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectActivityAssignment>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfProjectActivitiesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /project-activities/paged").ConfigureAwait(false);

        var items = new List<ProjectActivityAssignment>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.ProjectActivity>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfProjectActivitiesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /project-activities/count");

    internal static ProjectActivityAssignment Map(Raw.ProjectActivity source) => new()
    {
        Number = source.Number,
        ProjectNumber = source.ProjectNumber,
        ActivityNumber = source.ActivityNumber,
        StartDate = source.StartDate,
        EndDate = source.EndDate,
        ResponsibleEmployeeNumber = source.ResponsibleEmployeeNumber,
        Completed = source.Completed,
        ObjectVersion = source.ObjectVersion,
        LastUpdated = source.LastUpdated,
    };

    internal static Raw.ProjectActivity ToGenerated(ProjectActivityAssignment source) => new()
    {
        Number = source.Number,
        ProjectNumber = source.ProjectNumber,
        ActivityNumber = source.ActivityNumber,
        StartDate = source.StartDate,
        EndDate = source.EndDate,
        ResponsibleEmployeeNumber = source.ResponsibleEmployeeNumber,
        Completed = source.Completed,
        ObjectVersion = source.ObjectVersion,
        LastUpdated = source.LastUpdated,
    };
}

/// <summary>
/// <c>/project-activities</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectActivityAssignmentResource
{
    private readonly Raw.ProjectActivitiesClient _client;

    internal ProjectActivityAssignmentResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ProjectActivitiesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectActivityAssignment, ProjectActivityAssignmentFilter, ProjectActivityAssignmentSort> AsQuery() => new(new ProjectActivityAssignmentSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectActivityAssignment, ProjectActivityAssignmentFilter, ProjectActivityAssignmentSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectActivityAssignmentFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectActivityAssignment, ProjectActivityAssignmentFilter, ProjectActivityAssignmentSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectActivityAssignment, ProjectActivityAssignmentFilter, ProjectActivityAssignmentSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectActivityAssignmentSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectActivityAssignment, ProjectActivityAssignmentFilter, ProjectActivityAssignmentSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectActivityAssignmentSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectActivityAssignment, ProjectActivityAssignmentFilter, ProjectActivityAssignmentSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectActivityAssignment> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectActivityAssignment>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectActivityAssignment> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetProjectActivityByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /project-activities/{number}").ConfigureAwait(false);

        return ProjectActivityAssignmentSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectActivityAssignment item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateProjectActivityAsync(body: ProjectActivityAssignmentSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /project-activities").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectActivityAssignment item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateProjectActivityByIdAsync(body: ProjectActivityAssignmentSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /project-activities");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteProjectActivityByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /project-activities/{number}");
}

/// <summary>
/// A resource from <c>/project-customers</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectCustomer
{
    /// <summary>The <c>number</c> field.</summary>
    public required int Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/project-customers</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectCustomerFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;
}

/// <summary>
/// What <c>/project-customers</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectCustomerSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public EconomicSortField Name { get; } = null!;
}

/// <summary>Fetches <c>/project-customers</c> and maps it to <see cref="ProjectCustomer"/>.</summary>
internal sealed class ProjectCustomerSource(Raw.ProjectCustomersClient client)
    : IEconomicOpenSource<ProjectCustomer>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectCustomer>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllProjectCustomersAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /project-customers").ConfigureAwait(false);

        var items = new List<ProjectCustomer>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectCustomer>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectCustomer>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfProjectCustomersAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /project-customers/paged").ConfigureAwait(false);

        var items = new List<ProjectCustomer>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.ProjectCustomer>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfProjectCustomersAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /project-customers/count");

    internal static ProjectCustomer Map(Raw.ProjectCustomer source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.ProjectCustomer ToGenerated(ProjectCustomer source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/project-customers</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectCustomerResource
{
    private readonly Raw.ProjectCustomersClient _client;

    internal ProjectCustomerResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ProjectCustomersClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectCustomer, ProjectCustomerFilter, ProjectCustomerSort> AsQuery() => new(new ProjectCustomerSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectCustomer, ProjectCustomerFilter, ProjectCustomerSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectCustomerFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectCustomer, ProjectCustomerFilter, ProjectCustomerSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectCustomer, ProjectCustomerFilter, ProjectCustomerSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectCustomerSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectCustomer, ProjectCustomerFilter, ProjectCustomerSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectCustomerSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectCustomer, ProjectCustomerFilter, ProjectCustomerSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectCustomer> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectCustomer>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectCustomer> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetProjectCustomerByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /project-customers/{number}").ConfigureAwait(false);

        return ProjectCustomerSource.Map(response);
    }
}

/// <summary>
/// A resource from <c>/project-employees</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectEmployeeDetails
{
    /// <summary>The <c>number</c> field.</summary>
    public required int Number { get; init; }

    /// <summary>The <c>groupNumber</c> field.</summary>
    public required int GroupNumber { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>isUser</c> field.</summary>
    public bool? IsUser { get; init; }

    /// <summary>The <c>userId</c> field.</summary>
    public string? UserId { get; init; }

    /// <summary>The <c>canApprove</c> field.</summary>
    public required bool CanApprove { get; init; }

    /// <summary>The <c>employeeType</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int EmployeeType { get; init; }

    /// <summary>The <c>canInvoice</c> field.</summary>
    public required bool CanInvoice { get; init; }

    /// <summary>The <c>isBarred</c> field.</summary>
    public required bool IsBarred { get; init; }

    /// <summary>The <c>address</c> field.</summary>
    public string? Address { get; init; }

    /// <summary>The <c>city</c> field.</summary>
    public string? City { get; init; }

    /// <summary>The <c>zipCode</c> field.</summary>
    public string? ZipCode { get; init; }

    /// <summary>The <c>cutOffDate</c> field.</summary>
    public System.DateTimeOffset? CutOffDate { get; init; }

    /// <summary>The <c>salesPriceBefore</c> field.</summary>
    public double? SalesPriceBefore { get; init; }

    /// <summary>The <c>salesPriceAfter</c> field.</summary>
    public double? SalesPriceAfter { get; init; }

    /// <summary>The <c>costPriceBefore</c> field.</summary>
    public double? CostPriceBefore { get; init; }

    /// <summary>The <c>costPriceAfter</c> field.</summary>
    public double? CostPriceAfter { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/project-employees</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectEmployeeDetailsFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>groupNumber</c>.</summary>
    [EconomicField("groupNumber")]
    public NumericField<int> GroupNumber { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>isUser</c>.</summary>
    [EconomicField("isUser")]
    public EqualityField<bool> IsUser { get; } = null!;

    /// <summary>Maps to <c>userId</c>.</summary>
    [EconomicField("userId")]
    public EqualityField<string> UserId { get; } = null!;

    /// <summary>Maps to <c>employeeType</c>.</summary>
    [EconomicField("employeeType")]
    public EqualityField<int> EmployeeType { get; } = null!;

    /// <summary>Maps to <c>canInvoice</c>.</summary>
    [EconomicField("canInvoice")]
    public EqualityField<bool> CanInvoice { get; } = null!;

    /// <summary>Maps to <c>isBarred</c>.</summary>
    [EconomicField("isBarred")]
    public EqualityField<bool> IsBarred { get; } = null!;

    /// <summary>Maps to <c>address</c>.</summary>
    [EconomicField("address")]
    public EqualityField<string> Address { get; } = null!;

    /// <summary>Maps to <c>city</c>.</summary>
    [EconomicField("city")]
    public EqualityField<string> City { get; } = null!;

    /// <summary>Maps to <c>zipCode</c>.</summary>
    [EconomicField("zipCode")]
    public EqualityField<string> ZipCode { get; } = null!;

    /// <summary>Maps to <c>cutOffDate</c>.</summary>
    [EconomicField("cutOffDate")]
    public ComparableField<System.DateTimeOffset> CutOffDate { get; } = null!;

    /// <summary>Maps to <c>salesPriceBefore</c>.</summary>
    [EconomicField("salesPriceBefore")]
    public NumericField<double> SalesPriceBefore { get; } = null!;

    /// <summary>Maps to <c>salesPriceAfter</c>.</summary>
    [EconomicField("salesPriceAfter")]
    public NumericField<double> SalesPriceAfter { get; } = null!;

    /// <summary>Maps to <c>costPriceBefore</c>.</summary>
    [EconomicField("costPriceBefore")]
    public NumericField<double> CostPriceBefore { get; } = null!;

    /// <summary>Maps to <c>costPriceAfter</c>.</summary>
    [EconomicField("costPriceAfter")]
    public NumericField<double> CostPriceAfter { get; } = null!;
}

/// <summary>
/// What <c>/project-employees</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectEmployeeDetailsSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;

    /// <summary>Maps to <c>cutOffDate</c>.</summary>
    [EconomicField("cutOffDate")]
    public EconomicSortField CutOffDate { get; } = null!;

    /// <summary>Maps to <c>salesPriceBefore</c>.</summary>
    [EconomicField("salesPriceBefore")]
    public EconomicSortField SalesPriceBefore { get; } = null!;

    /// <summary>Maps to <c>salesPriceAfter</c>.</summary>
    [EconomicField("salesPriceAfter")]
    public EconomicSortField SalesPriceAfter { get; } = null!;

    /// <summary>Maps to <c>costPriceBefore</c>.</summary>
    [EconomicField("costPriceBefore")]
    public EconomicSortField CostPriceBefore { get; } = null!;

    /// <summary>Maps to <c>costPriceAfter</c>.</summary>
    [EconomicField("costPriceAfter")]
    public EconomicSortField CostPriceAfter { get; } = null!;
}

/// <summary>Fetches <c>/project-employees</c> and maps it to <see cref="ProjectEmployeeDetails"/>.</summary>
internal sealed class ProjectEmployeeDetailsSource(Raw.ProjectEmployeesClient client)
    : IEconomicOpenSource<ProjectEmployeeDetails>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectEmployeeDetails>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllProjectEmployeesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /project-employees").ConfigureAwait(false);

        var items = new List<ProjectEmployeeDetails>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectEmployeeDetails>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectEmployeeDetails>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfProjectEmployeesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /project-employees/paged").ConfigureAwait(false);

        var items = new List<ProjectEmployeeDetails>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.ProjectEmployee>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfProjectEmployeesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /project-employees/count");

    internal static ProjectEmployeeDetails Map(Raw.ProjectEmployee source) => new()
    {
        Number = source.Number,
        GroupNumber = source.GroupNumber,
        Name = source.Name,
        IsUser = source.IsUser,
        UserId = source.UserId,
        CanApprove = source.CanApprove,
        EmployeeType = (int)source.EmployeeType,
        CanInvoice = source.CanInvoice,
        IsBarred = source.IsBarred,
        Address = source.Address,
        City = source.City,
        ZipCode = source.ZipCode,
        CutOffDate = source.CutOffDate,
        SalesPriceBefore = source.SalesPriceBefore,
        SalesPriceAfter = source.SalesPriceAfter,
        CostPriceBefore = source.CostPriceBefore,
        CostPriceAfter = source.CostPriceAfter,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.ProjectEmployee ToGenerated(ProjectEmployeeDetails source) => new()
    {
        Number = source.Number,
        GroupNumber = source.GroupNumber,
        Name = source.Name,
        IsUser = source.IsUser,
        UserId = source.UserId,
        CanApprove = source.CanApprove,
        EmployeeType = (Raw.EmployeeType)source.EmployeeType,
        CanInvoice = source.CanInvoice,
        IsBarred = source.IsBarred,
        Address = source.Address,
        City = source.City,
        ZipCode = source.ZipCode,
        CutOffDate = source.CutOffDate,
        SalesPriceBefore = source.SalesPriceBefore,
        SalesPriceAfter = source.SalesPriceAfter,
        CostPriceBefore = source.CostPriceBefore,
        CostPriceAfter = source.CostPriceAfter,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/project-employees</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectEmployeeDetailsResource
{
    private readonly Raw.ProjectEmployeesClient _client;

    internal ProjectEmployeeDetailsResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ProjectEmployeesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectEmployeeDetails, ProjectEmployeeDetailsFilter, ProjectEmployeeDetailsSort> AsQuery() => new(new ProjectEmployeeDetailsSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectEmployeeDetails, ProjectEmployeeDetailsFilter, ProjectEmployeeDetailsSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeDetailsFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectEmployeeDetails, ProjectEmployeeDetailsFilter, ProjectEmployeeDetailsSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectEmployeeDetails, ProjectEmployeeDetailsFilter, ProjectEmployeeDetailsSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeDetailsSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectEmployeeDetails, ProjectEmployeeDetailsFilter, ProjectEmployeeDetailsSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectEmployeeDetailsSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectEmployeeDetails, ProjectEmployeeDetailsFilter, ProjectEmployeeDetailsSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectEmployeeDetails> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectEmployeeDetails>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectEmployeeDetails> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetProjectEmployeeByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /project-employees/{number}").ConfigureAwait(false);

        return ProjectEmployeeDetailsSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectEmployeeDetails item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateProjectEmployeeAsync(body: ProjectEmployeeDetailsSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /project-employees").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectEmployeeDetails item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateProjectEmployeeByIdAsync(body: ProjectEmployeeDetailsSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /project-employees");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteProjectEmployeeByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /project-employees/{number}");
}

/// <summary>
/// A resource from <c>/ProjectGroups</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectGroup
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>type</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int Type { get; init; }

    /// <summary>The <c>includeCostPriceInFinance</c> field.</summary>
    public bool? IncludeCostPriceInFinance { get; init; }

    /// <summary>The <c>costAccountOngoingType</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int CostAccountOngoingType { get; init; }

    /// <summary>The <c>costAccountOngoing</c> field.</summary>
    public int? CostAccountOngoing { get; init; }

    /// <summary>The <c>costContraAccountOngoing</c> field.</summary>
    public int? CostContraAccountOngoing { get; init; }

    /// <summary>The <c>costAccountClosed</c> field.</summary>
    public int? CostAccountClosed { get; init; }

    /// <summary>The <c>includeSalesPriceInFinance</c> field.</summary>
    public bool? IncludeSalesPriceInFinance { get; init; }

    /// <summary>The <c>salesAccountOngoingType</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int SalesAccountOngoingType { get; init; }

    /// <summary>The <c>salesAccountOngoing</c> field.</summary>
    public int? SalesAccountOngoing { get; init; }

    /// <summary>The <c>salesContraAccountOngoing</c> field.</summary>
    public int? SalesContraAccountOngoing { get; init; }

    /// <summary>The <c>salesAccountClosed</c> field.</summary>
    public int? SalesAccountClosed { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/ProjectGroups</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectGroupFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>type</c>.</summary>
    [EconomicField("type")]
    public EqualityField<int> Type { get; } = null!;

    /// <summary>Maps to <c>includeCostPriceInFinance</c>.</summary>
    [EconomicField("includeCostPriceInFinance")]
    public EqualityField<bool> IncludeCostPriceInFinance { get; } = null!;

    /// <summary>Maps to <c>costAccountOngoing</c>.</summary>
    [EconomicField("costAccountOngoing")]
    public NumericField<int> CostAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>costContraAccountOngoing</c>.</summary>
    [EconomicField("costContraAccountOngoing")]
    public NumericField<int> CostContraAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>costAccountClosed</c>.</summary>
    [EconomicField("costAccountClosed")]
    public NumericField<int> CostAccountClosed { get; } = null!;

    /// <summary>Maps to <c>includeSalesPriceInFinance</c>.</summary>
    [EconomicField("includeSalesPriceInFinance")]
    public EqualityField<bool> IncludeSalesPriceInFinance { get; } = null!;

    /// <summary>Maps to <c>salesAccountOngoing</c>.</summary>
    [EconomicField("salesAccountOngoing")]
    public NumericField<int> SalesAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>salesContraAccountOngoing</c>.</summary>
    [EconomicField("salesContraAccountOngoing")]
    public NumericField<int> SalesContraAccountOngoing { get; } = null!;

    /// <summary>Maps to <c>salesAccountClosed</c>.</summary>
    [EconomicField("salesAccountClosed")]
    public NumericField<int> SalesAccountClosed { get; } = null!;
}

/// <summary>
/// What <c>/ProjectGroups</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectGroupSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/ProjectGroups</c> and maps it to <see cref="ProjectGroup"/>.</summary>
internal sealed class ProjectGroupSource(Raw.ProjectGroupsClient client)
    : IEconomicOpenSource<ProjectGroup>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectGroup>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllProjectGroupsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /ProjectGroups").ConfigureAwait(false);

        var items = new List<ProjectGroup>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectGroup>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectGroup>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfProjectGroupsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /ProjectGroups/paged").ConfigureAwait(false);

        var items = new List<ProjectGroup>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.ProjectGroup>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfProjectGroupsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /ProjectGroups/count");

    internal static ProjectGroup Map(Raw.ProjectGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (int)source.Type,
        IncludeCostPriceInFinance = source.IncludeCostPriceInFinance,
        CostAccountOngoingType = (int)source.CostAccountOngoingType,
        CostAccountOngoing = source.CostAccountOngoing,
        CostContraAccountOngoing = source.CostContraAccountOngoing,
        CostAccountClosed = source.CostAccountClosed,
        IncludeSalesPriceInFinance = source.IncludeSalesPriceInFinance,
        SalesAccountOngoingType = (int)source.SalesAccountOngoingType,
        SalesAccountOngoing = source.SalesAccountOngoing,
        SalesContraAccountOngoing = source.SalesContraAccountOngoing,
        SalesAccountClosed = source.SalesAccountClosed,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.ProjectGroup ToGenerated(ProjectGroup source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (Raw.ProjectGroupType)source.Type,
        IncludeCostPriceInFinance = source.IncludeCostPriceInFinance,
        CostAccountOngoingType = (Raw.OngoingAccountType)source.CostAccountOngoingType,
        CostAccountOngoing = source.CostAccountOngoing,
        CostContraAccountOngoing = source.CostContraAccountOngoing,
        CostAccountClosed = source.CostAccountClosed,
        IncludeSalesPriceInFinance = source.IncludeSalesPriceInFinance,
        SalesAccountOngoingType = (Raw.OngoingAccountType)source.SalesAccountOngoingType,
        SalesAccountOngoing = source.SalesAccountOngoing,
        SalesContraAccountOngoing = source.SalesContraAccountOngoing,
        SalesAccountClosed = source.SalesAccountClosed,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/ProjectGroups</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectGroupResource
{
    private readonly Raw.ProjectGroupsClient _client;

    internal ProjectGroupResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ProjectGroupsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectGroup, ProjectGroupFilter, ProjectGroupSort> AsQuery() => new(new ProjectGroupSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectGroup, ProjectGroupFilter, ProjectGroupSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectGroupFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectGroup, ProjectGroupFilter, ProjectGroupSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectGroup, ProjectGroupFilter, ProjectGroupSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectGroupSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectGroup, ProjectGroupFilter, ProjectGroupSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectGroupSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectGroup, ProjectGroupFilter, ProjectGroupSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectGroup> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectGroup>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectGroup> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetProjectGroupByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /ProjectGroups/{number}").ConfigureAwait(false);

        return ProjectGroupSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectGroup item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateProjectGroupAsync(body: ProjectGroupSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /ProjectGroups").ConfigureAwait(false);

        return response.Number;
    }
}

/// <summary>
/// A resource from <c>/ProjectStatuses</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectStatus
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public string? Name { get; init; }

    /// <summary>The <c>type</c> field.</summary>
    /// <remarks>
    /// One of the values e-conomic publishes for it. The specification defines them as
    /// numbers and gives them no names, and the server sends the number, so the number is
    /// what this carries rather than a name invented here.
    /// </remarks>
    public required int Type { get; init; }

    /// <summary>The <c>priority</c> field.</summary>
    public int? Priority { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/ProjectStatuses</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectStatusFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>type</c>.</summary>
    [EconomicField("type")]
    public EqualityField<int> Type { get; } = null!;

    /// <summary>Maps to <c>priority</c>.</summary>
    [EconomicField("priority")]
    public NumericField<int> Priority { get; } = null!;
}

/// <summary>
/// What <c>/ProjectStatuses</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectStatusSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/ProjectStatuses</c> and maps it to <see cref="ProjectStatus"/>.</summary>
internal sealed class ProjectStatusSource(Raw.ProjectStatusesClient client)
    : IEconomicOpenSource<ProjectStatus>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectStatus>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllProjectStatusesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /ProjectStatuses").ConfigureAwait(false);

        var items = new List<ProjectStatus>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectStatus>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectStatus>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfProjectStatusesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /ProjectStatuses/paged").ConfigureAwait(false);

        var items = new List<ProjectStatus>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.ProjectStatus>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfProjectStatusesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /ProjectStatuses/count");

    internal static ProjectStatus Map(Raw.ProjectStatus source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (int)source.Type,
        Priority = source.Priority,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.ProjectStatus ToGenerated(ProjectStatus source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Type = (Raw.ProjectStatusType)source.Type,
        Priority = source.Priority,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/ProjectStatuses</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectStatusResource
{
    private readonly Raw.ProjectStatusesClient _client;

    internal ProjectStatusResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.ProjectStatusesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectStatus, ProjectStatusFilter, ProjectStatusSort> AsQuery() => new(new ProjectStatusSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectStatus, ProjectStatusFilter, ProjectStatusSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectStatusFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectStatus, ProjectStatusFilter, ProjectStatusSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectStatus, ProjectStatusFilter, ProjectStatusSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectStatusSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectStatus, ProjectStatusFilter, ProjectStatusSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectStatusSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectStatus, ProjectStatusFilter, ProjectStatusSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectStatus> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectStatus>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectStatus> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetProjectStatusByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /ProjectStatuses/{number}").ConfigureAwait(false);

        return ProjectStatusSource.Map(response);
    }
}

/// <summary>
/// A resource from <c>/TimeEntries</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectTimeEntry
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>projectNumber</c> field.</summary>
    public required int ProjectNumber { get; init; }

    /// <summary>The <c>activityNumber</c> field.</summary>
    public required int ActivityNumber { get; init; }

    /// <summary>The <c>employeeNumber</c> field.</summary>
    public required int EmployeeNumber { get; init; }

    /// <summary>The <c>date</c> field.</summary>
    public required System.DateTimeOffset Date { get; init; }

    /// <summary>The <c>text</c> field.</summary>
    public string? Text { get; init; }

    /// <summary>The <c>numberOfHours</c> field.</summary>
    public double? NumberOfHours { get; init; }

    /// <summary>The <c>isApproved</c> field.</summary>
    public bool? IsApproved { get; init; }

    /// <summary>The <c>isReconciled</c> field.</summary>
    public bool? IsReconciled { get; init; }

    /// <summary>The <c>lastUpdated</c> field.</summary>
    public System.DateTimeOffset? LastUpdated { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/TimeEntries</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectTimeEntryFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>projectNumber</c>.</summary>
    [EconomicField("projectNumber")]
    public NumericField<int> ProjectNumber { get; } = null!;

    /// <summary>Maps to <c>activityNumber</c>.</summary>
    [EconomicField("activityNumber")]
    public NumericField<int> ActivityNumber { get; } = null!;

    /// <summary>Maps to <c>employeeNumber</c>.</summary>
    [EconomicField("employeeNumber")]
    public NumericField<int> EmployeeNumber { get; } = null!;

    /// <summary>Maps to <c>date</c>.</summary>
    [EconomicField("date")]
    public ComparableField<System.DateTimeOffset> Date { get; } = null!;

    /// <summary>Maps to <c>text</c>.</summary>
    [EconomicField("text")]
    public TextField Text { get; } = null!;

    /// <summary>Maps to <c>numberOfHours</c>.</summary>
    [EconomicField("numberOfHours")]
    public NumericField<double> NumberOfHours { get; } = null!;

    /// <summary>Maps to <c>isApproved</c>.</summary>
    [EconomicField("isApproved")]
    public EqualityField<bool> IsApproved { get; } = null!;

    /// <summary>Maps to <c>isReconciled</c>.</summary>
    [EconomicField("isReconciled")]
    public EqualityField<bool> IsReconciled { get; } = null!;

    /// <summary>Maps to <c>lastUpdated</c>.</summary>
    [EconomicField("lastUpdated")]
    public ComparableField<System.DateTimeOffset> LastUpdated { get; } = null!;
}

/// <summary>
/// What <c>/TimeEntries</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectTimeEntrySort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/TimeEntries</c> and maps it to <see cref="ProjectTimeEntry"/>.</summary>
internal sealed class ProjectTimeEntrySource(Raw.TimeEntriesClient client)
    : IEconomicOpenSource<ProjectTimeEntry>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectTimeEntry>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllTimeEntriesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /TimeEntries").ConfigureAwait(false);

        var items = new List<ProjectTimeEntry>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectTimeEntry>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectTimeEntry>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfTimeEntriesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /TimeEntries/paged").ConfigureAwait(false);

        var items = new List<ProjectTimeEntry>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.TimeEntry>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfTimeEntriesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /TimeEntries/count");

    internal static ProjectTimeEntry Map(Raw.TimeEntry source) => new()
    {
        Number = source.Number,
        ProjectNumber = source.ProjectNumber,
        ActivityNumber = source.ActivityNumber,
        EmployeeNumber = source.EmployeeNumber,
        Date = source.Date,
        Text = source.Text,
        NumberOfHours = source.NumberOfHours,
        IsApproved = source.IsApproved,
        IsReconciled = source.IsReconciled,
        LastUpdated = source.LastUpdated,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.TimeEntry ToGenerated(ProjectTimeEntry source) => new()
    {
        Number = source.Number,
        ProjectNumber = source.ProjectNumber,
        ActivityNumber = source.ActivityNumber,
        EmployeeNumber = source.EmployeeNumber,
        Date = source.Date,
        Text = source.Text,
        NumberOfHours = source.NumberOfHours,
        IsApproved = source.IsApproved,
        IsReconciled = source.IsReconciled,
        LastUpdated = source.LastUpdated,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/TimeEntries</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectTimeEntryResource
{
    private readonly Raw.TimeEntriesClient _client;

    internal ProjectTimeEntryResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.TimeEntriesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectTimeEntry, ProjectTimeEntryFilter, ProjectTimeEntrySort> AsQuery() => new(new ProjectTimeEntrySource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectTimeEntry, ProjectTimeEntryFilter, ProjectTimeEntrySort> Where(System.Linq.Expressions.Expression<System.Func<ProjectTimeEntryFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectTimeEntry, ProjectTimeEntryFilter, ProjectTimeEntrySort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectTimeEntry, ProjectTimeEntryFilter, ProjectTimeEntrySort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectTimeEntrySort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectTimeEntry, ProjectTimeEntryFilter, ProjectTimeEntrySort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectTimeEntrySort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectTimeEntry, ProjectTimeEntryFilter, ProjectTimeEntrySort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectTimeEntry> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectTimeEntry>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectTimeEntry> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetTimeEntryByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /TimeEntries/{number}").ConfigureAwait(false);

        return ProjectTimeEntrySource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(ProjectTimeEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateTimeEntryAsync(body: ProjectTimeEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /TimeEntries").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(ProjectTimeEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateTimeEntryByIdAsync(body: ProjectTimeEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /TimeEntries");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteTimeEntryByIdAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /TimeEntries/{number}");
}

/// <summary>
/// A resource from <c>/TimeEntryPrices</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record ProjectTimeEntryPrice
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>costPrice</c> field.</summary>
    public double? CostPrice { get; init; }

    /// <summary>The <c>salesPrice</c> field.</summary>
    public double? SalesPrice { get; init; }
}

/// <summary>
/// What <c>/TimeEntryPrices</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectTimeEntryPriceFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;
}

/// <summary>
/// What <c>/TimeEntryPrices</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class ProjectTimeEntryPriceSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/TimeEntryPrices</c> and maps it to <see cref="ProjectTimeEntryPrice"/>.</summary>
internal sealed class ProjectTimeEntryPriceSource(Raw.TimeEntryPricesClient client)
    : IEconomicOpenSource<ProjectTimeEntryPrice>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<ProjectTimeEntryPrice>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllTimeEntryPricesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /TimeEntryPrices").ConfigureAwait(false);

        var items = new List<ProjectTimeEntryPrice>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<ProjectTimeEntryPrice>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<ProjectTimeEntryPrice>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfTimeEntryPricesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /TimeEntryPrices/paged").ConfigureAwait(false);

        var items = new List<ProjectTimeEntryPrice>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.TimeEntryPrices>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        throw new NotSupportedException(
            "e-conomic publishes no count endpoint for /TimeEntryPrices, so this collection "
            + "cannot be counted. Enumerate it, or count a collection that publishes one.");

    internal static ProjectTimeEntryPrice Map(Raw.TimeEntryPrices source) => new()
    {
        Number = source.Number,
        CostPrice = source.CostPrice,
        SalesPrice = source.SalesPrice,
    };

    internal static Raw.TimeEntryPrices ToGenerated(ProjectTimeEntryPrice source) => new()
    {
        Number = source.Number,
        CostPrice = source.CostPrice,
        SalesPrice = source.SalesPrice,
    };
}

/// <summary>
/// <c>/TimeEntryPrices</c>, as a query and the writes the service supports.
/// </summary>
public sealed class ProjectTimeEntryPriceResource
{
    private readonly Raw.TimeEntryPricesClient _client;

    internal ProjectTimeEntryPriceResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.TimeEntryPricesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<ProjectTimeEntryPrice, ProjectTimeEntryPriceFilter, ProjectTimeEntryPriceSort> AsQuery() => new(new ProjectTimeEntryPriceSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectTimeEntryPrice, ProjectTimeEntryPriceFilter, ProjectTimeEntryPriceSort> Where(System.Linq.Expressions.Expression<System.Func<ProjectTimeEntryPriceFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<ProjectTimeEntryPrice, ProjectTimeEntryPriceFilter, ProjectTimeEntryPriceSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectTimeEntryPrice, ProjectTimeEntryPriceFilter, ProjectTimeEntryPriceSort> OrderBy(System.Linq.Expressions.Expression<System.Func<ProjectTimeEntryPriceSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<ProjectTimeEntryPrice, ProjectTimeEntryPriceFilter, ProjectTimeEntryPriceSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<ProjectTimeEntryPriceSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<ProjectTimeEntryPrice, ProjectTimeEntryPriceFilter, ProjectTimeEntryPriceSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<ProjectTimeEntryPrice> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<ProjectTimeEntryPrice>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<ProjectTimeEntryPrice> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetTimeEntryPricesByIdAsync(number: number, cancellationToken: cancellationToken),
            "GET /TimeEntryPrices/{number}").ConfigureAwait(false);

        return ProjectTimeEntryPriceSource.Map(response);
    }
}
