// <auto-generated>
//     Generated by tools/EConomic.SpecConverter (open-facade).
//     Regenerate after a spec refresh; do not edit by hand.
// </auto-generated>
#nullable enable

using EConomic.Querying;
using Raw = EConomic.Open.Generated.Dimensions;

namespace EConomic.Open;

/// <summary>
/// A resource from <c>/dimension-data/accounts</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionAccount
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public required int DimensionNumber { get; init; }

    /// <summary>The <c>dimensionKey</c> field.</summary>
    public required int DimensionKey { get; init; }

    /// <summary>The <c>isDistribution</c> field.</summary>
    public bool? IsDistribution { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>accountNumber</c> field.</summary>
    public required int AccountNumber { get; init; }
}

/// <summary>
/// What <c>/dimension-data/accounts</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionAccountFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public NumericField<int> DimensionKey { get; } = null!;

    /// <summary>Maps to <c>accountNumber</c>.</summary>
    [EconomicField("accountNumber")]
    public NumericField<int> AccountNumber { get; } = null!;
}

/// <summary>
/// What <c>/dimension-data/accounts</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionAccountSort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public EconomicSortField DimensionKey { get; } = null!;

    /// <summary>Maps to <c>accountNumber</c>.</summary>
    [EconomicField("accountNumber")]
    public EconomicSortField AccountNumber { get; } = null!;
}

/// <summary>Fetches <c>/dimension-data/accounts</c> and maps it to <see cref="DimensionAccount"/>.</summary>
internal sealed class DimensionAccountSource(Raw.AccountClient client)
    : IEconomicOpenSource<DimensionAccount>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionAccount>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfAccountsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/accounts").ConfigureAwait(false);

        var items = new List<DimensionAccount>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionAccount>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionAccount>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfAccountsPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimension-data/accounts/paged").ConfigureAwait(false);

        var items = new List<DimensionAccount>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Account>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetDimensionDataOfAccountsCountAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/accounts/count");

    internal static DimensionAccount Map(Raw.Account source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        AccountNumber = source.AccountNumber,
    };

    internal static Raw.Account ToGenerated(DimensionAccount source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        AccountNumber = source.AccountNumber,
    };
}

/// <summary>
/// <c>/dimension-data/accounts</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionAccountResource
{
    private readonly Raw.AccountClient _client;

    internal DimensionAccountResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.AccountClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionAccount, DimensionAccountFilter, DimensionAccountSort> AsQuery() => new(new DimensionAccountSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionAccount, DimensionAccountFilter, DimensionAccountSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionAccountFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionAccount, DimensionAccountFilter, DimensionAccountSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionAccount, DimensionAccountFilter, DimensionAccountSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionAccountSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionAccount, DimensionAccountFilter, DimensionAccountSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionAccountSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionAccount, DimensionAccountFilter, DimensionAccountSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionAccount> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionAccount>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(DimensionAccount item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateAccountDimensionDataAsync(body: DimensionAccountSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /dimension-data/accounts").ConfigureAwait(false);

        return response.DimensionNumber;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(DimensionAccount item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateAccountDimensionDataAsync(body: DimensionAccountSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /dimension-data/accounts");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="item">The record to delete.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    /// <remarks>
    /// This collection is deleted from by payload rather than by identifier, which is how
    /// e-conomic publishes it: the record's key is more than one part and none of it is in
    /// the path. Send back the record you read.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public Task DeleteAsync(DimensionAccount item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.DeleteAccountByIdAsync(body: DimensionAccountSource.ToGenerated(item), cancellationToken: cancellationToken),
            "DELETE /dimension-data/accounts");
    }
}

/// <summary>
/// A resource from <c>/dimension-data/booked-entries</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionBookedEntry
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public required int DimensionNumber { get; init; }

    /// <summary>The <c>dimensionKey</c> field.</summary>
    public required int DimensionKey { get; init; }

    /// <summary>The <c>isDistribution</c> field.</summary>
    public bool? IsDistribution { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>entryNumber</c> field.</summary>
    public required int EntryNumber { get; init; }
}

/// <summary>
/// What <c>/dimension-data/booked-entries</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionBookedEntryFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public NumericField<int> DimensionKey { get; } = null!;

    /// <summary>Maps to <c>entryNumber</c>.</summary>
    [EconomicField("entryNumber")]
    public NumericField<int> EntryNumber { get; } = null!;
}

/// <summary>
/// What <c>/dimension-data/booked-entries</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionBookedEntrySort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public EconomicSortField DimensionKey { get; } = null!;

    /// <summary>Maps to <c>entryNumber</c>.</summary>
    [EconomicField("entryNumber")]
    public EconomicSortField EntryNumber { get; } = null!;
}

/// <summary>Fetches <c>/dimension-data/booked-entries</c> and maps it to <see cref="DimensionBookedEntry"/>.</summary>
internal sealed class DimensionBookedEntrySource(Raw.BookedEntryClient client)
    : IEconomicOpenSource<DimensionBookedEntry>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionBookedEntry>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBookedEntriesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/booked-entries").ConfigureAwait(false);

        var items = new List<DimensionBookedEntry>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionBookedEntry>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionBookedEntry>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBookedEntriesPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimension-data/booked-entries/paged").ConfigureAwait(false);

        var items = new List<DimensionBookedEntry>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.BookedEntry>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBookedEntriesCountAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/booked-entries/count");

    internal static DimensionBookedEntry Map(Raw.BookedEntry source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        EntryNumber = source.EntryNumber,
    };

    internal static Raw.BookedEntry ToGenerated(DimensionBookedEntry source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        EntryNumber = source.EntryNumber,
    };
}

/// <summary>
/// <c>/dimension-data/booked-entries</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionBookedEntryResource
{
    private readonly Raw.BookedEntryClient _client;

    internal DimensionBookedEntryResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.BookedEntryClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionBookedEntry, DimensionBookedEntryFilter, DimensionBookedEntrySort> AsQuery() => new(new DimensionBookedEntrySource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionBookedEntry, DimensionBookedEntryFilter, DimensionBookedEntrySort> Where(System.Linq.Expressions.Expression<System.Func<DimensionBookedEntryFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionBookedEntry, DimensionBookedEntryFilter, DimensionBookedEntrySort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionBookedEntry, DimensionBookedEntryFilter, DimensionBookedEntrySort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionBookedEntrySort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionBookedEntry, DimensionBookedEntryFilter, DimensionBookedEntrySort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionBookedEntrySort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionBookedEntry, DimensionBookedEntryFilter, DimensionBookedEntrySort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionBookedEntry> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionBookedEntry>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(DimensionBookedEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateBookedEntryDimensionDataAsync(body: DimensionBookedEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /dimension-data/booked-entries").ConfigureAwait(false);

        return response.DimensionNumber;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(DimensionBookedEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateBookedEntryDimensionDataAsync(body: DimensionBookedEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /dimension-data/booked-entries");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="item">The record to delete.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    /// <remarks>
    /// This collection is deleted from by payload rather than by identifier, which is how
    /// e-conomic publishes it: the record's key is more than one part and none of it is in
    /// the path. Send back the record you read.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public Task DeleteAsync(DimensionBookedEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.DeleteBookedEntryByIdAsync(body: DimensionBookedEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "DELETE /dimension-data/booked-entries");
    }
}

/// <summary>
/// A resource from <c>/dimension-data/booked-invoice-lines</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionBookedInvoiceLine
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public required int DimensionNumber { get; init; }

    /// <summary>The <c>dimensionKey</c> field.</summary>
    public required int DimensionKey { get; init; }

    /// <summary>The <c>isDistribution</c> field.</summary>
    public bool? IsDistribution { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>invoiceNumber</c> field.</summary>
    public required int InvoiceNumber { get; init; }

    /// <summary>The <c>lineNumber</c> field.</summary>
    public required int LineNumber { get; init; }
}

/// <summary>
/// What <c>/dimension-data/booked-invoice-lines</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionBookedInvoiceLineFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public NumericField<int> DimensionKey { get; } = null!;

    /// <summary>Maps to <c>invoiceNumber</c>.</summary>
    [EconomicField("invoiceNumber")]
    public NumericField<int> InvoiceNumber { get; } = null!;

    /// <summary>Maps to <c>lineNumber</c>.</summary>
    [EconomicField("lineNumber")]
    public NumericField<int> LineNumber { get; } = null!;
}

/// <summary>
/// What <c>/dimension-data/booked-invoice-lines</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionBookedInvoiceLineSort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public EconomicSortField DimensionKey { get; } = null!;

    /// <summary>Maps to <c>invoiceNumber</c>.</summary>
    [EconomicField("invoiceNumber")]
    public EconomicSortField InvoiceNumber { get; } = null!;

    /// <summary>Maps to <c>lineNumber</c>.</summary>
    [EconomicField("lineNumber")]
    public EconomicSortField LineNumber { get; } = null!;
}

/// <summary>Fetches <c>/dimension-data/booked-invoice-lines</c> and maps it to <see cref="DimensionBookedInvoiceLine"/>.</summary>
internal sealed class DimensionBookedInvoiceLineSource(Raw.BookedInvoiceLineClient client)
    : IEconomicOpenSource<DimensionBookedInvoiceLine>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionBookedInvoiceLine>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBookedInvoiceLinesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/booked-invoice-lines").ConfigureAwait(false);

        var items = new List<DimensionBookedInvoiceLine>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionBookedInvoiceLine>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionBookedInvoiceLine>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBookedInvoiceLinesPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimension-data/booked-invoice-lines/paged").ConfigureAwait(false);

        var items = new List<DimensionBookedInvoiceLine>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.BookedInvoiceLine>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBookedInvoiceLinesCountAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/booked-invoice-lines/count");

    internal static DimensionBookedInvoiceLine Map(Raw.BookedInvoiceLine source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        InvoiceNumber = source.InvoiceNumber,
        LineNumber = source.LineNumber,
    };

    internal static Raw.BookedInvoiceLine ToGenerated(DimensionBookedInvoiceLine source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        InvoiceNumber = source.InvoiceNumber,
        LineNumber = source.LineNumber,
    };
}

/// <summary>
/// <c>/dimension-data/booked-invoice-lines</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionBookedInvoiceLineResource
{
    private readonly Raw.BookedInvoiceLineClient _client;

    internal DimensionBookedInvoiceLineResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.BookedInvoiceLineClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionBookedInvoiceLine, DimensionBookedInvoiceLineFilter, DimensionBookedInvoiceLineSort> AsQuery() => new(new DimensionBookedInvoiceLineSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionBookedInvoiceLine, DimensionBookedInvoiceLineFilter, DimensionBookedInvoiceLineSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionBookedInvoiceLineFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionBookedInvoiceLine, DimensionBookedInvoiceLineFilter, DimensionBookedInvoiceLineSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionBookedInvoiceLine, DimensionBookedInvoiceLineFilter, DimensionBookedInvoiceLineSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionBookedInvoiceLineSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionBookedInvoiceLine, DimensionBookedInvoiceLineFilter, DimensionBookedInvoiceLineSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionBookedInvoiceLineSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionBookedInvoiceLine, DimensionBookedInvoiceLineFilter, DimensionBookedInvoiceLineSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionBookedInvoiceLine> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionBookedInvoiceLine>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);
}

/// <summary>
/// A resource from <c>/dimension-data/budget-figures</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionBudgetFigure
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public required int DimensionNumber { get; init; }

    /// <summary>The <c>dimensionKey</c> field.</summary>
    public required int DimensionKey { get; init; }

    /// <summary>The <c>isDistribution</c> field.</summary>
    public bool? IsDistribution { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>budgetFigureNumber</c> field.</summary>
    public required int BudgetFigureNumber { get; init; }
}

/// <summary>
/// What <c>/dimension-data/budget-figures</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionBudgetFigureFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public NumericField<int> DimensionKey { get; } = null!;

    /// <summary>Maps to <c>budgetFigureNumber</c>.</summary>
    [EconomicField("budgetFigureNumber")]
    public NumericField<int> BudgetFigureNumber { get; } = null!;
}

/// <summary>
/// What <c>/dimension-data/budget-figures</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionBudgetFigureSort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public EconomicSortField DimensionKey { get; } = null!;

    /// <summary>Maps to <c>budgetFigureNumber</c>.</summary>
    [EconomicField("budgetFigureNumber")]
    public EconomicSortField BudgetFigureNumber { get; } = null!;
}

/// <summary>Fetches <c>/dimension-data/budget-figures</c> and maps it to <see cref="DimensionBudgetFigure"/>.</summary>
internal sealed class DimensionBudgetFigureSource(Raw.BudgetFigureClient client)
    : IEconomicOpenSource<DimensionBudgetFigure>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionBudgetFigure>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBudgetFiguresAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/budget-figures").ConfigureAwait(false);

        var items = new List<DimensionBudgetFigure>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionBudgetFigure>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionBudgetFigure>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBudgetFiguresPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimension-data/budget-figures/paged").ConfigureAwait(false);

        var items = new List<DimensionBudgetFigure>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.BudgetFigure>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetDimensionDataOfBudgetFiguresCountAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/budget-figures/count");

    internal static DimensionBudgetFigure Map(Raw.BudgetFigure source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        BudgetFigureNumber = source.BudgetFigureNumber,
    };

    internal static Raw.BudgetFigure ToGenerated(DimensionBudgetFigure source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        BudgetFigureNumber = source.BudgetFigureNumber,
    };
}

/// <summary>
/// <c>/dimension-data/budget-figures</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionBudgetFigureResource
{
    private readonly Raw.BudgetFigureClient _client;

    internal DimensionBudgetFigureResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.BudgetFigureClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionBudgetFigure, DimensionBudgetFigureFilter, DimensionBudgetFigureSort> AsQuery() => new(new DimensionBudgetFigureSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionBudgetFigure, DimensionBudgetFigureFilter, DimensionBudgetFigureSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionBudgetFigureFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionBudgetFigure, DimensionBudgetFigureFilter, DimensionBudgetFigureSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionBudgetFigure, DimensionBudgetFigureFilter, DimensionBudgetFigureSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionBudgetFigureSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionBudgetFigure, DimensionBudgetFigureFilter, DimensionBudgetFigureSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionBudgetFigureSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionBudgetFigure, DimensionBudgetFigureFilter, DimensionBudgetFigureSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionBudgetFigure> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionBudgetFigure>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(DimensionBudgetFigure item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateBudgetFigureDimensionDataAsync(body: DimensionBudgetFigureSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /dimension-data/budget-figures").ConfigureAwait(false);

        return response.DimensionNumber;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(DimensionBudgetFigure item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateBudgetFigureDimensionDataAsync(body: DimensionBudgetFigureSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /dimension-data/budget-figures");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="item">The record to delete.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    /// <remarks>
    /// This collection is deleted from by payload rather than by identifier, which is how
    /// e-conomic publishes it: the record's key is more than one part and none of it is in
    /// the path. Send back the record you read.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public Task DeleteAsync(DimensionBudgetFigure item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.DeleteBudgetFigureByIdAsync(body: DimensionBudgetFigureSource.ToGenerated(item), cancellationToken: cancellationToken),
            "DELETE /dimension-data/budget-figures");
    }
}

/// <summary>
/// A resource from <c>/dimensions</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record Dimension
{
    /// <summary>The <c>number</c> field.</summary>
    public int? Number { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>active</c> field.</summary>
    public required bool Active { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/dimensions</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionFilter
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public NumericField<int> Number { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>active</c>.</summary>
    [EconomicField("active")]
    public EqualityField<bool> Active { get; } = null!;
}

/// <summary>
/// What <c>/dimensions</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionSort
{
    /// <summary>Maps to <c>number</c>.</summary>
    [EconomicField("number")]
    public EconomicSortField Number { get; } = null!;
}

/// <summary>Fetches <c>/dimensions</c> and maps it to <see cref="Dimension"/>.</summary>
internal sealed class DimensionSource(Raw.DimensionsClient client)
    : IEconomicOpenSource<Dimension>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<Dimension>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllDimensionsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimensions").ConfigureAwait(false);

        var items = new List<Dimension>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<Dimension>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<Dimension>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfDimensionsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimensions/paged").ConfigureAwait(false);

        var items = new List<Dimension>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Dimension>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfDimensionsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimensions/count");

    internal static Dimension Map(Raw.Dimension source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Active = source.Active,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.Dimension ToGenerated(Dimension source) => new()
    {
        Number = source.Number,
        Name = source.Name,
        Active = source.Active,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/dimensions</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionResource
{
    private readonly Raw.DimensionsClient _client;

    internal DimensionResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.DimensionsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<Dimension, DimensionFilter, DimensionSort> AsQuery() => new(new DimensionSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<Dimension, DimensionFilter, DimensionSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<Dimension, DimensionFilter, DimensionSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<Dimension, DimensionFilter, DimensionSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<Dimension, DimensionFilter, DimensionSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<Dimension, DimensionFilter, DimensionSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<Dimension> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<Dimension>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<Dimension> GetAsync(int number, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetDimensionAsync(number: number, cancellationToken: cancellationToken),
            "GET /dimensions/{number}").ConfigureAwait(false);

        return DimensionSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(Dimension item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateDimensionAsync(body: DimensionSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /dimensions").ConfigureAwait(false);

        return response.Number;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(Dimension item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateDimensionAsync(body: DimensionSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /dimensions");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="number">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(int number, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteDimensionAsync(number: number, cancellationToken: cancellationToken),
            "DELETE /dimensions/{number}");
}

/// <summary>
/// A resource from <c>/distributions</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionDistribution
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public int? DimensionNumber { get; init; }

    /// <summary>The <c>key</c> field.</summary>
    public int? Key { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public string? Name { get; init; }

    /// <summary>The <c>active</c> field.</summary>
    public bool? Active { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/distributions</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionDistributionFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>key</c>.</summary>
    [EconomicField("key")]
    public NumericField<int> Key { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>active</c>.</summary>
    [EconomicField("active")]
    public EqualityField<bool> Active { get; } = null!;
}

/// <summary>
/// What <c>/distributions</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionDistributionSort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>key</c>.</summary>
    [EconomicField("key")]
    public EconomicSortField Key { get; } = null!;
}

/// <summary>Fetches <c>/distributions</c> and maps it to <see cref="DimensionDistribution"/>.</summary>
internal sealed class DimensionDistributionSource(Raw.DimensionDistributionsClient client)
    : IEconomicOpenSource<DimensionDistribution>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionDistribution>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllDimensionDistributionsAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /distributions").ConfigureAwait(false);

        var items = new List<DimensionDistribution>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionDistribution>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionDistribution>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfDimensionDistributionsAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /distributions/paged").ConfigureAwait(false);

        var items = new List<DimensionDistribution>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.DimensionDistribution>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfDimensionDistributionsAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /distributions/count");

    internal static DimensionDistribution Map(Raw.DimensionDistribution source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        Key = source.Key,
        Name = source.Name,
        Active = source.Active,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.DimensionDistribution ToGenerated(DimensionDistribution source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        Key = source.Key,
        Name = source.Name,
        Active = source.Active,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/distributions</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionDistributionResource
{
    private readonly Raw.DimensionDistributionsClient _client;

    internal DimensionDistributionResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.DimensionDistributionsClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionDistribution, DimensionDistributionFilter, DimensionDistributionSort> AsQuery() => new(new DimensionDistributionSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionDistribution, DimensionDistributionFilter, DimensionDistributionSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionDistributionFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionDistribution, DimensionDistributionFilter, DimensionDistributionSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionDistribution, DimensionDistributionFilter, DimensionDistributionSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionDistributionSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionDistribution, DimensionDistributionFilter, DimensionDistributionSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionDistributionSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionDistribution, DimensionDistributionFilter, DimensionDistributionSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionDistribution> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionDistribution>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);
}

/// <summary>
/// A resource from <c>/values</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionValue
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public int? DimensionNumber { get; init; }

    /// <summary>The <c>key</c> field.</summary>
    public int? Key { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public string? Name { get; init; }

    /// <summary>The <c>active</c> field.</summary>
    public bool? Active { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/values</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionValueFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>key</c>.</summary>
    [EconomicField("key")]
    public NumericField<int> Key { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>active</c>.</summary>
    [EconomicField("active")]
    public EqualityField<bool> Active { get; } = null!;
}

/// <summary>
/// What <c>/values</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionValueSort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>key</c>.</summary>
    [EconomicField("key")]
    public EconomicSortField Key { get; } = null!;
}

/// <summary>Fetches <c>/values</c> and maps it to <see cref="DimensionValue"/>.</summary>
internal sealed class DimensionValueSource(Raw.DimensionValuesClient client)
    : IEconomicOpenSource<DimensionValue>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionValue>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllDimensionValuesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /values").ConfigureAwait(false);

        var items = new List<DimensionValue>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionValue>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionValue>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetPageOfDimensionValuesAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /values/paged").ConfigureAwait(false);

        var items = new List<DimensionValue>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.DimensionValue>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfDimensionValuesAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /values/count");

    internal static DimensionValue Map(Raw.DimensionValue source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        Key = source.Key,
        Name = source.Name,
        Active = source.Active,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.DimensionValue ToGenerated(DimensionValue source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        Key = source.Key,
        Name = source.Name,
        Active = source.Active,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/values</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionValueResource
{
    private readonly Raw.DimensionValuesClient _client;

    internal DimensionValueResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.DimensionValuesClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionValue, DimensionValueFilter, DimensionValueSort> AsQuery() => new(new DimensionValueSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionValue, DimensionValueFilter, DimensionValueSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionValueFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionValue, DimensionValueFilter, DimensionValueSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionValue, DimensionValueFilter, DimensionValueSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionValueSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionValue, DimensionValueFilter, DimensionValueSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionValueSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionValue, DimensionValueFilter, DimensionValueSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionValue> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionValue>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(DimensionValue item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateDimensionValueAsync(body: DimensionValueSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /values").ConfigureAwait(false);

        return response.Key;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(DimensionValue item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateDimensionValueByIdAsync(body: DimensionValueSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /values");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="item">The record to delete.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    /// <remarks>
    /// This collection is deleted from by payload rather than by identifier, which is how
    /// e-conomic publishes it: the record's key is more than one part and none of it is in
    /// the path. Send back the record you read.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public Task DeleteAsync(DimensionValue item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.DeleteDimensionValueAsync(body: DimensionValueSource.ToGenerated(item), cancellationToken: cancellationToken),
            "DELETE /values");
    }
}

/// <summary>
/// A resource from <c>/dimension-data/draft-entries</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionDraftEntry
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public required int DimensionNumber { get; init; }

    /// <summary>The <c>dimensionKey</c> field.</summary>
    public required int DimensionKey { get; init; }

    /// <summary>The <c>isDistribution</c> field.</summary>
    public bool? IsDistribution { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>journalNumber</c> field.</summary>
    public required int JournalNumber { get; init; }

    /// <summary>The <c>entryNumber</c> field.</summary>
    public required int EntryNumber { get; init; }
}

/// <summary>
/// What <c>/dimension-data/draft-entries</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionDraftEntryFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public NumericField<int> DimensionKey { get; } = null!;

    /// <summary>Maps to <c>journalNumber</c>.</summary>
    [EconomicField("journalNumber")]
    public NumericField<int> JournalNumber { get; } = null!;

    /// <summary>Maps to <c>entryNumber</c>.</summary>
    [EconomicField("entryNumber")]
    public NumericField<int> EntryNumber { get; } = null!;
}

/// <summary>
/// What <c>/dimension-data/draft-entries</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionDraftEntrySort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public EconomicSortField DimensionKey { get; } = null!;

    /// <summary>Maps to <c>journalNumber</c>.</summary>
    [EconomicField("journalNumber")]
    public EconomicSortField JournalNumber { get; } = null!;

    /// <summary>Maps to <c>entryNumber</c>.</summary>
    [EconomicField("entryNumber")]
    public EconomicSortField EntryNumber { get; } = null!;
}

/// <summary>Fetches <c>/dimension-data/draft-entries</c> and maps it to <see cref="DimensionDraftEntry"/>.</summary>
internal sealed class DimensionDraftEntrySource(Raw.DraftEntryClient client)
    : IEconomicOpenSource<DimensionDraftEntry>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionDraftEntry>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfDraftEntriesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/draft-entries").ConfigureAwait(false);

        var items = new List<DimensionDraftEntry>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionDraftEntry>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionDraftEntry>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfDraftEntriesPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimension-data/draft-entries/paged").ConfigureAwait(false);

        var items = new List<DimensionDraftEntry>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.DraftEntry>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetDimensionDataOfDraftEntriesCountAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/draft-entries/count");

    internal static DimensionDraftEntry Map(Raw.DraftEntry source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        JournalNumber = source.JournalNumber,
        EntryNumber = source.EntryNumber,
    };

    internal static Raw.DraftEntry ToGenerated(DimensionDraftEntry source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        JournalNumber = source.JournalNumber,
        EntryNumber = source.EntryNumber,
    };
}

/// <summary>
/// <c>/dimension-data/draft-entries</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionDraftEntryResource
{
    private readonly Raw.DraftEntryClient _client;

    internal DimensionDraftEntryResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.DraftEntryClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionDraftEntry, DimensionDraftEntryFilter, DimensionDraftEntrySort> AsQuery() => new(new DimensionDraftEntrySource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionDraftEntry, DimensionDraftEntryFilter, DimensionDraftEntrySort> Where(System.Linq.Expressions.Expression<System.Func<DimensionDraftEntryFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionDraftEntry, DimensionDraftEntryFilter, DimensionDraftEntrySort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionDraftEntry, DimensionDraftEntryFilter, DimensionDraftEntrySort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionDraftEntrySort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionDraftEntry, DimensionDraftEntryFilter, DimensionDraftEntrySort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionDraftEntrySort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionDraftEntry, DimensionDraftEntryFilter, DimensionDraftEntrySort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionDraftEntry> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionDraftEntry>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(DimensionDraftEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateDraftEntryDimensionDataAsync(body: DimensionDraftEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /dimension-data/draft-entries").ConfigureAwait(false);

        return response.DimensionNumber;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(DimensionDraftEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateDraftEntryDimensionDataAsync(body: DimensionDraftEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /dimension-data/draft-entries");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="item">The record to delete.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    /// <remarks>
    /// This collection is deleted from by payload rather than by identifier, which is how
    /// e-conomic publishes it: the record's key is more than one part and none of it is in
    /// the path. Send back the record you read.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public Task DeleteAsync(DimensionDraftEntry item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.DeleteDraftEntryByIdAsync(body: DimensionDraftEntrySource.ToGenerated(item), cancellationToken: cancellationToken),
            "DELETE /dimension-data/draft-entries");
    }
}

/// <summary>
/// A resource from <c>/dimension-data/sales-document-lines</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record DimensionSalesDocumentLine
{
    /// <summary>The <c>dimensionNumber</c> field.</summary>
    public required int DimensionNumber { get; init; }

    /// <summary>The <c>dimensionKey</c> field.</summary>
    public required int DimensionKey { get; init; }

    /// <summary>The <c>isDistribution</c> field.</summary>
    public bool? IsDistribution { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }

    /// <summary>The <c>documentNumber</c> field.</summary>
    public required int DocumentNumber { get; init; }

    /// <summary>The <c>lineNumber</c> field.</summary>
    public required int LineNumber { get; init; }
}

/// <summary>
/// What <c>/dimension-data/sales-document-lines</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionSalesDocumentLineFilter
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public NumericField<int> DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public NumericField<int> DimensionKey { get; } = null!;

    /// <summary>Maps to <c>documentNumber</c>.</summary>
    [EconomicField("documentNumber")]
    public NumericField<int> DocumentNumber { get; } = null!;

    /// <summary>Maps to <c>lineNumber</c>.</summary>
    [EconomicField("lineNumber")]
    public NumericField<int> LineNumber { get; } = null!;
}

/// <summary>
/// What <c>/dimension-data/sales-document-lines</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class DimensionSalesDocumentLineSort
{
    /// <summary>Maps to <c>dimensionNumber</c>.</summary>
    [EconomicField("dimensionNumber")]
    public EconomicSortField DimensionNumber { get; } = null!;

    /// <summary>Maps to <c>dimensionKey</c>.</summary>
    [EconomicField("dimensionKey")]
    public EconomicSortField DimensionKey { get; } = null!;

    /// <summary>Maps to <c>documentNumber</c>.</summary>
    [EconomicField("documentNumber")]
    public EconomicSortField DocumentNumber { get; } = null!;

    /// <summary>Maps to <c>lineNumber</c>.</summary>
    [EconomicField("lineNumber")]
    public EconomicSortField LineNumber { get; } = null!;
}

/// <summary>Fetches <c>/dimension-data/sales-document-lines</c> and maps it to <see cref="DimensionSalesDocumentLine"/>.</summary>
internal sealed class DimensionSalesDocumentLineSource(Raw.SalesDocumentLineClient client)
    : IEconomicOpenSource<DimensionSalesDocumentLine>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<DimensionSalesDocumentLine>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfSalesDocumentLinesAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/sales-document-lines").ConfigureAwait(false);

        var items = new List<DimensionSalesDocumentLine>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<DimensionSalesDocumentLine>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<DimensionSalesDocumentLine>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetDimensionDataOfSalesDocumentLinesPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /dimension-data/sales-document-lines/paged").ConfigureAwait(false);

        var items = new List<DimensionSalesDocumentLine>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.SalesDocumentLine>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetDimensionDataOfSalesDocumentLinesCountAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /dimension-data/sales-document-lines/count");

    internal static DimensionSalesDocumentLine Map(Raw.SalesDocumentLine source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        DocumentNumber = source.DocumentNumber,
        LineNumber = source.LineNumber,
    };

    internal static Raw.SalesDocumentLine ToGenerated(DimensionSalesDocumentLine source) => new()
    {
        DimensionNumber = source.DimensionNumber,
        DimensionKey = source.DimensionKey,
        IsDistribution = source.IsDistribution,
        ObjectVersion = source.ObjectVersion,
        DocumentNumber = source.DocumentNumber,
        LineNumber = source.LineNumber,
    };
}

/// <summary>
/// <c>/dimension-data/sales-document-lines</c>, as a query and the writes the service supports.
/// </summary>
public sealed class DimensionSalesDocumentLineResource
{
    private readonly Raw.SalesDocumentLineClient _client;

    internal DimensionSalesDocumentLineResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.SalesDocumentLineClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<DimensionSalesDocumentLine, DimensionSalesDocumentLineFilter, DimensionSalesDocumentLineSort> AsQuery() => new(new DimensionSalesDocumentLineSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionSalesDocumentLine, DimensionSalesDocumentLineFilter, DimensionSalesDocumentLineSort> Where(System.Linq.Expressions.Expression<System.Func<DimensionSalesDocumentLineFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<DimensionSalesDocumentLine, DimensionSalesDocumentLineFilter, DimensionSalesDocumentLineSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionSalesDocumentLine, DimensionSalesDocumentLineFilter, DimensionSalesDocumentLineSort> OrderBy(System.Linq.Expressions.Expression<System.Func<DimensionSalesDocumentLineSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<DimensionSalesDocumentLine, DimensionSalesDocumentLineFilter, DimensionSalesDocumentLineSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<DimensionSalesDocumentLineSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<DimensionSalesDocumentLine, DimensionSalesDocumentLineFilter, DimensionSalesDocumentLineSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<DimensionSalesDocumentLine> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<DimensionSalesDocumentLine>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<int?> CreateAsync(DimensionSalesDocumentLine item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateSalesDocumentLineDimensionDataAsync(body: DimensionSalesDocumentLineSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /dimension-data/sales-document-lines").ConfigureAwait(false);

        return response.DimensionNumber;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(DimensionSalesDocumentLine item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateSalesDocumentLineDimensionDataAsync(body: DimensionSalesDocumentLineSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /dimension-data/sales-document-lines");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="item">The record to delete.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    /// <remarks>
    /// This collection is deleted from by payload rather than by identifier, which is how
    /// e-conomic publishes it: the record's key is more than one part and none of it is in
    /// the path. Send back the record you read.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public Task DeleteAsync(DimensionSalesDocumentLine item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.DeleteSalesDocumentLineByIdAsync(body: DimensionSalesDocumentLineSource.ToGenerated(item), cancellationToken: cancellationToken),
            "DELETE /dimension-data/sales-document-lines");
    }
}
