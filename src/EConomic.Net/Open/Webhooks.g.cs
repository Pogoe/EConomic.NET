// <auto-generated>
//     Generated by tools/EConomic.SpecConverter (open-facade).
//     Regenerate after a spec refresh; do not edit by hand.
// </auto-generated>
#nullable enable

using EConomic.Querying;
using Raw = EConomic.Open.Generated.Webhooks;

namespace EConomic.Open;

/// <summary>
/// A resource from <c>/webhooks</c>.
/// </summary>
/// <remarks>
/// The same shape is used to read, create and update: these services describe a resource once
/// and accept it back unchanged. Properties the server maintains are simply left unset.
/// </remarks>
public sealed record Webhook
{
    /// <summary>The <c>eventType</c> field.</summary>
    public required string EventType { get; init; }

    /// <summary>The <c>name</c> field.</summary>
    public required string Name { get; init; }

    /// <summary>The <c>url</c> field.</summary>
    public required string Url { get; init; }

    /// <summary>The <c>contentType</c> field.</summary>
    public required string ContentType { get; init; }

    /// <summary>The <c>postData</c> field.</summary>
    public string? PostData { get; init; }

    /// <summary>The <c>isDisabled</c> field.</summary>
    public bool? IsDisabled { get; init; }

    /// <summary>The <c>lastDisabledDate</c> field.</summary>
    public System.DateTimeOffset? LastDisabledDate { get; init; }

    /// <summary>The <c>lastUpdated</c> field.</summary>
    public System.DateTimeOffset? LastUpdated { get; init; }

    /// <summary>The <c>objectVersion</c> field.</summary>
    public string? ObjectVersion { get; init; }
}

/// <summary>
/// What <c>/webhooks</c> can be filtered on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class WebhookFilter
{
    /// <summary>Maps to <c>eventType</c>.</summary>
    [EconomicField("eventType")]
    public EqualityField<string> EventType { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public TextField Name { get; } = null!;

    /// <summary>Maps to <c>url</c>.</summary>
    [EconomicField("url")]
    public TextField Url { get; } = null!;

    /// <summary>Maps to <c>isDisabled</c>.</summary>
    [EconomicField("isDisabled")]
    public EqualityField<bool> IsDisabled { get; } = null!;

    /// <summary>Maps to <c>lastDisabledDate</c>.</summary>
    [EconomicField("lastDisabledDate")]
    public ComparableField<System.DateTimeOffset> LastDisabledDate { get; } = null!;

    /// <summary>Maps to <c>lastUpdated</c>.</summary>
    [EconomicField("lastUpdated")]
    public ComparableField<System.DateTimeOffset> LastUpdated { get; } = null!;
}

/// <summary>
/// What <c>/webhooks</c> can be sorted on.
/// </summary>
/// <remarks>
/// Each property is typed to the operators the service publishes for it, narrowed to those the
/// value's type can carry: e-conomic restricts <c>$in:</c> to numbers and <c>$like:</c> to text,
/// whatever an individual property claims. Anything absent here is not filterable at all, which
/// is most of them.
/// </remarks>
public sealed class WebhookSort
{
    /// <summary>Maps to <c>eventType</c>.</summary>
    [EconomicField("eventType")]
    public EconomicSortField EventType { get; } = null!;

    /// <summary>Maps to <c>name</c>.</summary>
    [EconomicField("name")]
    public EconomicSortField Name { get; } = null!;
}

/// <summary>Fetches <c>/webhooks</c> and maps it to <see cref="Webhook"/>.</summary>
internal sealed class WebhookSource(Raw.WebhooksClient client)
    : IEconomicOpenSource<Webhook>
{
    public bool CanCursor => true;

    public bool CanPage => true;

    public async Task<EconomicCursorPage<Webhook>> GetCursorPageAsync(
        string? cursor, string? filter, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetAllWebhooksAsync(cursor: cursor, filter: filter, cancellationToken: cancellationToken),
            "GET /webhooks").ConfigureAwait(false);

        var items = new List<Webhook>(response.Items?.Count ?? 0);
        foreach (var item in response.Items ?? [])
        {
            items.Add(Map(item));
        }

        return new EconomicCursorPage<Webhook>(items, response.Cursor);
    }

    public async Task<IReadOnlyList<Webhook>> GetPageAsync(
        string? filter, string? sort, int pageSize, int skipPages, CancellationToken cancellationToken)
    {
        var response = await OpenTransport.SendAsync(
            () => client.GetWebhooksPagedAsync(filter: filter, sort: sort, pageSize: pageSize, skipPages: skipPages, cancellationToken: cancellationToken),
            "GET /webhooks/paged").ConfigureAwait(false);

        var items = new List<Webhook>(response?.Count ?? 0);
        foreach (var item in response ?? Array.Empty<Raw.Webhook>())
        {
            items.Add(Map(item));
        }

        return items;
    }

    public Task<int> CountAsync(string? filter, CancellationToken cancellationToken) =>
        OpenTransport.SendAsync(
            () => client.GetNumberOfWebhooksAsync(filter: filter, cancellationToken: cancellationToken),
            "GET /webhooks/count");

    internal static Webhook Map(Raw.Webhook source) => new()
    {
        EventType = source.EventType,
        Name = source.Name,
        Url = source.Url,
        ContentType = source.ContentType,
        PostData = source.PostData,
        IsDisabled = source.IsDisabled,
        LastDisabledDate = source.LastDisabledDate,
        LastUpdated = source.LastUpdated,
        ObjectVersion = source.ObjectVersion,
    };

    internal static Raw.Webhook ToGenerated(Webhook source) => new()
    {
        EventType = source.EventType,
        Name = source.Name,
        Url = source.Url,
        ContentType = source.ContentType,
        PostData = source.PostData,
        IsDisabled = source.IsDisabled,
        LastDisabledDate = source.LastDisabledDate,
        LastUpdated = source.LastUpdated,
        ObjectVersion = source.ObjectVersion,
    };
}

/// <summary>
/// <c>/webhooks</c>, as a query and the writes the service supports.
/// </summary>
public sealed class WebhookResource
{
    private readonly Raw.WebhooksClient _client;

    internal WebhookResource(System.Net.Http.HttpClient httpClient, System.Uri baseAddress)
    {
        _client = new Raw.WebhooksClient(httpClient) { BaseUrl = baseAddress.ToString() };
    }

    /// <summary>This collection as a composable query.</summary>
    /// <returns>A query with no filter or ordering.</returns>
    public EconomicOpenQuery<Webhook, WebhookFilter, WebhookSort> AsQuery() => new(new WebhookSource(_client));

    /// <summary>Restricts what is returned.</summary>
    /// <param name="predicate">A filter over the filterable properties.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<Webhook, WebhookFilter, WebhookSort> Where(System.Linq.Expressions.Expression<System.Func<WebhookFilter, bool>> predicate) => AsQuery().Where(predicate);

    /// <summary>Restricts what is returned, using e-conomic's filter syntax directly.</summary>
    /// <param name="filter">A filter expression.</param>
    /// <returns>A query carrying the filter.</returns>
    public EconomicOpenQuery<Webhook, WebhookFilter, WebhookSort> WhereRaw(string filter) => AsQuery().WhereRaw(filter);

    /// <summary>Orders ascending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<Webhook, WebhookFilter, WebhookSort> OrderBy(System.Linq.Expressions.Expression<System.Func<WebhookSort, EconomicSortField>> selector) => AsQuery().OrderBy(selector);

    /// <summary>Orders descending, which moves the query onto the paged endpoint.</summary>
    /// <param name="selector">The property to sort by.</param>
    /// <returns>A query carrying the ordering.</returns>
    public EconomicOpenQuery<Webhook, WebhookFilter, WebhookSort> OrderByDescending(System.Linq.Expressions.Expression<System.Func<WebhookSort, EconomicSortField>> selector) => AsQuery().OrderByDescending(selector);

    /// <summary>Sets how many items are fetched per request.</summary>
    /// <param name="pageSize">Items per page, up to 1000.</param>
    /// <returns>A query using that page size.</returns>
    public EconomicOpenQuery<Webhook, WebhookFilter, WebhookSort> WithPageSize(int pageSize) => AsQuery().WithPageSize(pageSize);

    /// <summary>Enumerates everything, following the cursor.</summary>
    /// <param name="cancellationToken">Cancels the enumeration.</param>
    /// <returns>The items.</returns>
    public System.Collections.Generic.IAsyncEnumerable<Webhook> AsAsyncEnumerable(CancellationToken cancellationToken = default) => AsQuery().AsAsyncEnumerable(cancellationToken);

    /// <summary>Counts everything in the collection.</summary>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The number of records.</returns>
    public Task<int> CountAsync(CancellationToken cancellationToken = default) => AsQuery().CountAsync(cancellationToken);

    /// <summary>Fetches one numbered page.</summary>
    /// <param name="pageIndex">Zero-based page index.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The page.</returns>
    public Task<IReadOnlyList<Webhook>> GetPageAsync(int pageIndex, CancellationToken cancellationToken = default) => AsQuery().GetPageAsync(pageIndex, cancellationToken);

    /// <summary>Fetches one record.</summary>
    /// <param name="eventType">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The record.</returns>
    public async Task<Webhook> GetAsync(string eventType, CancellationToken cancellationToken = default)
    {
        var response = await OpenTransport.SendAsync(
            () => _client.GetWebhookAsync(eventType: eventType, cancellationToken: cancellationToken),
            "GET /webhooks/{eventType}").ConfigureAwait(false);

        return WebhookSource.Map(response);
    }

    /// <summary>Creates a record.</summary>
    /// <param name="item">The record to create, whose identifier you supply.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>The identifier e-conomic stored it under.</returns>
    /// <remarks>
    /// The response carries the identifier and nothing else, so that is what this returns —
    /// unlike the legacy REST API, whose creates answer with the whole resource. Read it back
    /// with <c>GetAsync</c> if you need the stored record, which also gives you the
    /// <c>objectVersion</c> an update requires.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    public async Task<string?> CreateAsync(Webhook item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        var response = await OpenTransport.SendAsync(
            () => _client.CreateWebhookAsync(body: WebhookSource.ToGenerated(item), cancellationToken: cancellationToken),
            "POST /webhooks").ConfigureAwait(false);

        return response.EventType;
    }

    /// <summary>Replaces a record.</summary>
    /// <param name="item">The replacement state, carrying the <c>objectVersion</c> that was read.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is replaced.</returns>
    /// <remarks>
    /// Updates are read-modify-write on this surface. The <c>objectVersion</c> from the record
    /// you read has to come back with it; a stale one, or none, is rejected with
    /// <see cref="Exceptions.EconomicConcurrencyException"/> and nothing is written.
    /// </remarks>
    /// <exception cref="System.ArgumentNullException"><paramref name="item"/> is <see langword="null"/>.</exception>
    /// <exception cref="Exceptions.EconomicConcurrencyException">The record changed since it was read.</exception>
    public Task UpdateAsync(Webhook item, CancellationToken cancellationToken = default)
    {
        System.ArgumentNullException.ThrowIfNull(item);

        return OpenTransport.SendAsync(
            () => _client.UpdateWebhookAsync(body: WebhookSource.ToGenerated(item), cancellationToken: cancellationToken),
            "PUT /webhooks");
    }

    /// <summary>Deletes a record.</summary>
    /// <param name="eventType">Its identifier.</param>
    /// <param name="cancellationToken">Cancels the request.</param>
    /// <returns>A task that completes once the record is gone.</returns>
    public Task DeleteAsync(string eventType, CancellationToken cancellationToken = default) =>
        OpenTransport.SendAsync(
            () => _client.DeleteWebhookAsync(eventType: eventType, cancellationToken: cancellationToken),
            "DELETE /webhooks/{eventType}");
}
