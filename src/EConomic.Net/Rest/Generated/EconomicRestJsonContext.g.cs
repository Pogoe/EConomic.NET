// <auto-generated>
//     Generated by tools/EConomic.SpecConverter (json-context).
//     Regenerate after running NSwag; do not edit by hand.
// </auto-generated>
#nullable enable

using System.Text.Json;
using System.Text.Json.Serialization;

namespace EConomic.Rest.Generated;

/// <summary>
/// Source-generated serialization metadata for the generated legacy clients, so the
/// package stays trim- and AOT-compatible despite NSwag emitting reflection-based calls.
/// </summary>
#if NET9_0_OR_GREATER
[JsonSourceGenerationOptions(
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
    PropertyNameCaseInsensitive = true,
    UseStringEnumConverter = true)]
#else
[JsonSourceGenerationOptions(
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
    PropertyNameCaseInsensitive = true)]
#endif
[JsonSerializable(typeof(AccountDetails))]
[JsonSerializable(typeof(AccountingYear))]
[JsonSerializable(typeof(AccountingYearPeriod))]
[JsonSerializable(typeof(AccountingYearPeriodTotals))]
[JsonSerializable(typeof(AccountingYearPeriodsCollection))]
[JsonSerializable(typeof(AccountingYearTotals))]
[JsonSerializable(typeof(AccountingYears))]
[JsonSerializable(typeof(AccountingYearsCollection))]
[JsonSerializable(typeof(AccountingYearsEntriesCollection))]
[JsonSerializable(typeof(AccountingYearsPOST))]
[JsonSerializable(typeof(AccountsAccountingYearPeriodTotals))]
[JsonSerializable(typeof(AccountsAccountingYearPeriodsCollection))]
[JsonSerializable(typeof(AccountsAccountingYearTotals))]
[JsonSerializable(typeof(AccountsCollection))]
[JsonSerializable(typeof(AccountsEntriesCollection))]
[JsonSerializable(typeof(AppRoles))]
[JsonSerializable(typeof(AppRolesCollection))]
[JsonSerializable(typeof(ArchivedOrderCollection))]
[JsonSerializable(typeof(ArchivedQuoteCollection))]
[JsonSerializable(typeof(BookRequestTemplate))]
[JsonSerializable(typeof(BookedInvoice))]
[JsonSerializable(typeof(BookedInvoiceCollection))]
[JsonSerializable(typeof(BookedInvoiceTotals))]
[JsonSerializable(typeof(CompanyBankInformationPUT))]
[JsonSerializable(typeof(CompanyPUT))]
[JsonSerializable(typeof(CurrencyCollection))]
[JsonSerializable(typeof(CurrencyDetails))]
[JsonSerializable(typeof(Customer))]
[JsonSerializable(typeof(CustomerBookedInvoicesCollection))]
[JsonSerializable(typeof(CustomerCollection))]
[JsonSerializable(typeof(CustomerContactDetails))]
[JsonSerializable(typeof(CustomerContactPOST))]
[JsonSerializable(typeof(CustomerContactPUT))]
[JsonSerializable(typeof(CustomerDetails))]
[JsonSerializable(typeof(CustomerDraftInvoiceTotals))]
[JsonSerializable(typeof(CustomerDraftInvoicesCollection))]
[JsonSerializable(typeof(CustomerGroup))]
[JsonSerializable(typeof(CustomerGroupCollection))]
[JsonSerializable(typeof(CustomerGroupCustomerCollection))]
[JsonSerializable(typeof(CustomerGroupPOST))]
[JsonSerializable(typeof(CustomerGroupPUT))]
[JsonSerializable(typeof(CustomerInvoices))]
[JsonSerializable(typeof(CustomerPOST))]
[JsonSerializable(typeof(CustomerPUT))]
[JsonSerializable(typeof(CustomerTemplates))]
[JsonSerializable(typeof(CustomerTotals))]
[JsonSerializable(typeof(CustomersCustomerContact))]
[JsonSerializable(typeof(CustomersDraftInvoiceLineTemplate))]
[JsonSerializable(typeof(DeliveryLocation))]
[JsonSerializable(typeof(DeliveryLocationCollection))]
[JsonSerializable(typeof(DeliveryLocationPOST))]
[JsonSerializable(typeof(DeliveryLocationPUT))]
[JsonSerializable(typeof(DepartmentDetails))]
[JsonSerializable(typeof(DepartmentalDistribution))]
[JsonSerializable(typeof(DepartmentalDistributionCollection))]
[JsonSerializable(typeof(DepartmentalDistributionSummaryCollection))]
[JsonSerializable(typeof(DepartmentsCollection))]
[JsonSerializable(typeof(DraftInvoice))]
[JsonSerializable(typeof(DraftInvoiceCollection))]
[JsonSerializable(typeof(DraftInvoiceLineTemplatesCollection))]
[JsonSerializable(typeof(DraftInvoiceLinesPost))]
[JsonSerializable(typeof(DraftInvoicePost))]
[JsonSerializable(typeof(DraftInvoiceTemplate))]
[JsonSerializable(typeof(DraftInvoiceTemplates))]
[JsonSerializable(typeof(DraftInvoiceTotals))]
[JsonSerializable(typeof(DraftInvoiceTotalsForAccountingYear))]
[JsonSerializable(typeof(DraftInvoiceTotalsForPeriod))]
[JsonSerializable(typeof(DraftInvoiceUpdate))]
[JsonSerializable(typeof(DraftOrderCollection))]
[JsonSerializable(typeof(DraftOrderPUT))]
[JsonSerializable(typeof(DraftOrderPost))]
[JsonSerializable(typeof(DraftQuoteCollection))]
[JsonSerializable(typeof(DraftQuotePUT))]
[JsonSerializable(typeof(DraftQuotePost))]
[JsonSerializable(typeof(EmployeeCollection))]
[JsonSerializable(typeof(EmployeeDetails))]
[JsonSerializable(typeof(EmployeeDraftInvoiceTotals))]
[JsonSerializable(typeof(EmployeeDraftInvoiceTotalsSummary))]
[JsonSerializable(typeof(EmployeesCustomersCollection))]
[JsonSerializable(typeof(EntryCollection))]
[JsonSerializable(typeof(ForBookingAnInvoice))]
[JsonSerializable(typeof(Invoice))]
[JsonSerializable(typeof(InvoiceTotals))]
[JsonSerializable(typeof(InvoicesBookedInvoiceCollection))]
[JsonSerializable(typeof(InvoicesDraftInvoiceTotals))]
[JsonSerializable(typeof(InvoicesForBookingAnInvoice))]
[JsonSerializable(typeof(InvoicesPaidBookedInvoiceTotals))]
[JsonSerializable(typeof(JournalCollection))]
[JsonSerializable(typeof(JournalDetails))]
[JsonSerializable(typeof(JournalEntryOnAVoucher))]
[JsonSerializable(typeof(JournalVoucher))]
[JsonSerializable(typeof(JournalVoucherPost))]
[JsonSerializable(typeof(JournalsFileMetaData))]
[JsonSerializable(typeof(JournalsForGETingAJournalEntry))]
[JsonSerializable(typeof(JournalsForGETingEntries))]
[JsonSerializable(typeof(JournalsForGETingTemplates))]
[JsonSerializable(typeof(JournalsForTheFinanceVoucherTemplate))]
[JsonSerializable(typeof(JournalsForTheManualCustomerInvoiceTemplate))]
[JsonSerializable(typeof(Layout))]
[JsonSerializable(typeof(LayoutsCollection))]
[JsonSerializable(typeof(NotOverdueUnpaidBookedInvoiceTotals))]
[JsonSerializable(typeof(Orders))]
[JsonSerializable(typeof(OrdersArchivedOrder))]
[JsonSerializable(typeof(OrdersDraftOrder))]
[JsonSerializable(typeof(OrdersForGETingTemplates))]
[JsonSerializable(typeof(OrdersSentOrder))]
[JsonSerializable(typeof(OverdueInvoiceCollection))]
[JsonSerializable(typeof(OverdueUnpaidBookedInvoiceTotals))]
[JsonSerializable(typeof(PaidBookedInvoiceTotals))]
[JsonSerializable(typeof(PaidInvoiceCollection))]
[JsonSerializable(typeof(PaymentTerm))]
[JsonSerializable(typeof(PaymentTermPOST))]
[JsonSerializable(typeof(PaymentTermUpdate))]
[JsonSerializable(typeof(PaymentTerms))]
[JsonSerializable(typeof(PaymentTermsCollection))]
[JsonSerializable(typeof(PaymentTypeCollection))]
[JsonSerializable(typeof(PaymentTypeYear))]
[JsonSerializable(typeof(Product))]
[JsonSerializable(typeof(ProductCollection))]
[JsonSerializable(typeof(ProductGroups))]
[JsonSerializable(typeof(ProductGroupsAccountsCollection))]
[JsonSerializable(typeof(ProductGroupsCollection))]
[JsonSerializable(typeof(ProductPriceCollection))]
[JsonSerializable(typeof(ProductPrices))]
[JsonSerializable(typeof(ProductPricesCreate))]
[JsonSerializable(typeof(ProductPricesUpdate))]
[JsonSerializable(typeof(Products))]
[JsonSerializable(typeof(ProductsCreate))]
[JsonSerializable(typeof(ProductsUpdate))]
[JsonSerializable(typeof(Quotes))]
[JsonSerializable(typeof(QuotesArchivedQuote))]
[JsonSerializable(typeof(QuotesDraftQuote))]
[JsonSerializable(typeof(QuotesForGETingTemplates))]
[JsonSerializable(typeof(QuotesSentQuote))]
[JsonSerializable(typeof(Self))]
[JsonSerializable(typeof(SentInvoice))]
[JsonSerializable(typeof(SentInvoiceCollection))]
[JsonSerializable(typeof(SentOrderCollection))]
[JsonSerializable(typeof(SentQuoteCollection))]
[JsonSerializable(typeof(Supplier))]
[JsonSerializable(typeof(SupplierResource))]
[JsonSerializable(typeof(SuppliersCollection))]
[JsonSerializable(typeof(SuppliersPostResponse))]
[JsonSerializable(typeof(SuppliersPutResponse))]
[JsonSerializable(typeof(Unit))]
[JsonSerializable(typeof(UnitDetails))]
[JsonSerializable(typeof(UnitUpdate))]
[JsonSerializable(typeof(UnitsCollection))]
[JsonSerializable(typeof(UnitsCreate))]
[JsonSerializable(typeof(UnpaidInvoiceCollection))]
[JsonSerializable(typeof(UserPUT))]
[JsonSerializable(typeof(VatAccount))]
[JsonSerializable(typeof(VatAccountsCollection))]
[JsonSerializable(typeof(VatTypes))]
[JsonSerializable(typeof(VatTypesCollection))]
[JsonSerializable(typeof(VatZone))]
[JsonSerializable(typeof(VatZoneCollection))]
[JsonSerializable(typeof(object))]
internal sealed partial class EconomicRestJsonContext : JsonSerializerContext;

/// <summary>
/// Points each generated client at the source-generated resolver. NSwag declares
/// <c>UpdateJsonSerializerSettings</c> as a partial hook for exactly this purpose.
/// </summary>
internal partial class AccountingYearsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class AccountsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class AppRolesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class CurrenciesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class CustomerGroupsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class CustomersClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class DepartmentalDistributionsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class DepartmentsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class EmployeesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class InvoicesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class JournalsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class LayoutsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class OrdersClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class PaymentTermsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class PaymentTypesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class ProductGroupsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class ProductsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class QuotesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class SelfClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class SuppliersClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class UnitsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class VatAccountsClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class VatTypesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

internal partial class VatZonesClient
{
    static partial void UpdateJsonSerializerSettings(JsonSerializerOptions settings)
    {
#if !NET9_0_OR_GREATER
        settings.Converters.Add(new JsonStringEnumConverter());
#endif

        // e-conomic rejects an explicit null: sending "address": null fails schema
        // validation with "Expected String but got Null", so an unset optional property
        // must be omitted rather than written. This has to be set on these settings, not
        // only through JsonSourceGenerationOptions: those apply to the context's own
        // Default instance, while the generated clients serialize through options of
        // their own that merely borrow the resolver.
        settings.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;

        settings.TypeInfoResolverChain.Add(EconomicRestJsonContext.Default);
    }
}

